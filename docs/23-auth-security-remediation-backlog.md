# 23. Authentication and Security Remediation Backlog

## Authority, baseline, and counting method

This is the actionable closure backlog derived from the current canonical `pixelsmith-auth-reference`
machine contract and the production-path re-audit recorded in document 22. The reference artifacts report
contract `2.0.0`, machine schema `14.0.0`, validator `11.0.0`, mapping schema `1.0.0`, and portable-config
schema `2.0.0`. The audit independently expanded grouped matrix labels and reproduced **155 unique
canonical AUTH IDs**: **110 verified, 23 implemented-but-unverified, 15 partial, 0 missing, and 7
not-applicable**. Consequently, the table below has exactly **38 applicable non-verified controls**.

`EXTERNAL/MANUAL` is a closure/evidence boundary in this document, not a sixth mutually exclusive status
in document 22. A control can have repository implementation verified while its deployed configuration or
provider exercise remains external. No statement here is a production-readiness claim.

## Row-level closure backlog

Each row is one canonical ID. Locations and callers name the current production path, not a parallel test
store. “Existing evidence” is deliberately candid when it is only source inspection.

| Canonical ID | Canonical requirement | Current classification | Exact unmet property or evidence deficiency | Production implementation location | Actual production caller/path | Persistence/config location | Existing behavioral evidence | Why current evidence is insufficient | Closure type | Severity | Exploitability | Exact acceptance criteria | Required behavioral test | Recommended remediation slice | Dependencies | Blocks application-code readiness? | Blocks production deployment validation? |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| AUTH-STORAGE-002 | Authorize and purpose-bind every server-side write. | implemented-but-unverified | No single test proves *every* write path; architecturally consistent but not exhaustively tested as one property. | All DB writes gated behind `'server-only'` modules with authorization/purpose checks first (e.g. `lib/membership/account-recovery.ts`, `lib/auth/session-registry.ts`) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | multiple tables | integration suite broadly | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | medium | defense-in-depth | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 3 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-SESSION-007 | Provide policy-defined visibility and revocation for active sessions. | implemented-but-unverified | Production implementation exists, but no behavioral test exercises the actual production path. | `lib/auth/session-registry.ts:116-134` `listActiveSessions` (per-session revocability by `sessionId`) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `idoc.auth_sessions` | integration coverage only (no e2e for multi-session UI found beyond `revokeOtherUserSessionsWithEvidence`) | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | medium | defense-in-depth | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 2 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-COOKIE-002 | Use host-only production session cookies with the `__Host-` contract. | implemented-but-unverified | `__Host-` prefix only takes effect in production; the e2e test explicitly covers the *development* cookie name, not the `__Host-` production variant — production behavior is code-verified but not e2e-proven in this suite. | `lib/auth/session-tokens.ts:16` `PRODUCTION_SESSION_COOKIE_NAME='__Host-idoc-session'`, no Domain set, `path:'/'` | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | N/A | `tests/security-e2e/cookies-and-headers.spec.ts:49` (dev-mode contract only, per test title) | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | medium | defense-in-depth | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 2 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-CSRF-003 | Issue, validate, expire, and rotate unpredictable CSRF tokens where the token control applies. | partial | The requirement specifically describes token-based CSRF defense (unpredictable, server-validated, expiring/rotating tokens); IDOC's Origin-check mechanism is a legitimate alternative but does not literally satisfy "tokens…expired/rotated." | Not implemented as classic CSRF tokens — IDOC relies entirely on Origin-header validation (`middleware.ts` + Next.js's built-in Server Action check), not on issued/rotated CSRF tokens | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | N/A | `tests/security-e2e/csrf.spec.ts` | Existing coverage cannot prove the named absent/incomplete mandatory property. | CODE+TEST | high | direct | Implement the missing property and prove it through the real production path. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 4 | Production database/provider/configuration where named; otherwise existing test harness. | yes | no |
| AUTH-ERROR-001 | Return stable, generic authentication errors without sensitive distinctions. | implemented-but-unverified | Production implementation exists, but no behavioral test exercises the actual production path. | Generic messages for persistent failures, e.g. `app/(login)/sign-in/actions.ts:74` "We could not finish signing you in automatically. Contact IDOC for help." | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | N/A | none found (no test asserts error-message class taxonomy) | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | medium | defense-in-depth | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 7 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-IDENTITY-003 | Apply canonical identity normalization while preserving an appropriate display form. | partial | Case-folding is plain `.toLowerCase()`, not full Unicode-aware case folding (e.g., no `.normalize()`/locale casing for non-ASCII local parts); "preserve display form" is not implemented — normalized (lowercased) form is what's stored and shown, original casing is discarded. | `lib/membership/validation.ts:86-88` `normalizeEmail` (trim + ASCII lowercase); no dot/plus stripping | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `lib/db/schema.ts:23,33` `users.email` unique + `uniqueIndex('users_normalized_email_unique') on lower(email)` | `tests/rate-limit-normalization.integration.ts` (behavioral, real Postgres) proves case/whitespace variants collapse to one identity | Existing coverage cannot prove the named absent/incomplete mandatory property. | CODE+TEST | medium | defense-in-depth | Implement the missing property and prove it through the real production path. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 6 | Production database/provider/configuration where named; otherwise existing test harness. | yes | no |
| AUTH-LIFECYCLE-002 | Reject authentication and duplicate registration for disallowed account lifecycle states. | implemented-but-unverified | Logic looks correct and DB-enforced, but no behavioral test drives a `suspended`/`deleted` account through `signIn` to confirm rejection end-to-end. | `app/(login)/actions.ts:61` (`signIn`, accountState allowlist `['active','onboarding','migrated_pending']`); `app/(login)/sign-up/actions.ts:87-88` (existing-email check) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `lib/db/migrations/0024_account_state_check_constraint.sql` (DB-level CHECK on `account_state`) | `tests/password-security-hardening.test.ts` (source-inspection only for the DB constraint); no integration test exercising login with a `suspended`/`deleted` account | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | medium | defense-in-depth | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 3 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-EMAIL-002 | Throttle and supersede email-verification OTP resend without enabling enumeration. | partial | Cooldown + supersession code exists and is exercised indirectly, but no test specifically drives `issueEmailOtp` resend/cooldown timing or confirms enumeration-resistant response shape for signup resend. | `lib/auth/email-otp.ts:67-69` (30s cooldown), `:73-74` (supersede prior unconsumed code) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | same table | `tests/email-otp-cross-purpose.integration.ts` partially; no dedicated resend-throttle/enumeration-resistance integration test found | Existing coverage cannot prove the named absent/incomplete mandatory property. | TEST | medium | defense-in-depth | Implement the missing property and prove it through the real production path. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 7 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-STORAGE-005 | Detect obsolete password hashes and upgrade them after successful authentication. | implemented-but-unverified | No pepper used (acceptable — optional per requirement). Rehash logic itself untested against real Postgres/`signIn`. | `lib/auth/password-hash.ts:90-92` (`passwordHashNeedsUpgrade`); `app/(login)/actions.ts:65-70` (rehash-on-successful-login, conditioned on unchanged hash via `WHERE ... eq(passwordHash, foundUser.passwordHash)`) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `users.password_hash` self-describing format string | `tests/password-hash.test.ts` (behavioral, unit) proves detection; no integration test exercises the actual DB rehash-on-login path | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | medium | defense-in-depth | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 5 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-PASSWORD-005 | Require authenticated subject binding, fresh verification, and session rotation for password changes. | implemented-but-unverified | Subject from authenticated session (`validatedActionWithUser`), fresh step-up required, current-password re-verification, `sessionVersion` rotation + `clearSession`, notification outbox — all present in code, but not behaviorally tested. | `app/(login)/actions.ts:192-226` (`updatePassword`) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `users.password_hash`, `auth_security_notification_outbox` | `tests/password-security-hardening.test.ts` (source-inspection only) — no integration test found exercising `updatePassword` against real Postgres | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | high | conditional | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 3 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-MFA-005 | Keep enrollment pending until proof succeeds and activate the factor atomically. | implemented-but-unverified | Pending factor is expiring (`TOTP_ENROLLMENT_TTL_MS = 10min`), purpose-bound, and displaying the QR/provisioning URI (`beginTotpEnrollment`) never activates it — only `finalizeInitialAuthenticatorEnrollment`/`finalizeAuthenticatorReplacement` do, atomically. No behavioral (real-Postgres) test exists for the actual production functions. | `lib/auth/mfa/totp.ts:140-190` (`beginTotpEnrollment`, CSPRNG via `generateTotpSecret`→`randomBytes(20)`); atomic activation in `lib/auth/mfa/enrollment-finalization.ts:10-75` and `lib/auth/mfa/replacement-finalization.ts:10-105` (production path) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `idoc.mfa_factors`, `idoc.mfa_enrollment_transactions` (`lib/db/schema.ts:446-491`) | `tests/security-completeness-item-8.test.ts`, `tests/live-privileged-mfa.test.ts` (source-inspection only for the production finalization files); `tests/mfa-store.integration.ts` (real-Postgres, but tests the parallel unused `consumeEnrollmentAndActivate` path) | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | high | conditional | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 2 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-STORAGE-006 | Encrypt MFA seed material and prevent its disclosure through client or telemetry boundaries. | implemented-but-unverified | Encryption is real (32-byte key required, GCM auth tag). No behavioral test of "never in logs/telemetry" for TOTP secrets specifically (only `console.*` absence confirmed by code review — no `console.*` calls anywhere in `lib/auth/mfa/`). | `lib/auth/mfa/totp.ts:116-138` (`encryptTotpSecret`/`decryptTotpSecret`, AES-256-GCM, per-key-ID) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `idoc.mfa_factors.encrypted_secret`/`encryption_key_id`; DB CHECK `mfa_factors_totp_secret_check` (`lib/db/schema.ts:467-469`) forces non-null secret+key for `totp` factors | `tests/canonical-mfa-runtime.test.ts` (unit round-trip); no test scans logs/browser storage for TOTP secrets specifically | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | medium | defense-in-depth | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 5 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-RECOVERY-004 | Generate high-entropy, one-way-stored, single-use recovery codes and consume them atomically. | implemented-but-unverified | Same architectural split as AUTH-MFA-005: `consumeRecoveryCodeWithEvidence` (production, called from `app/(login)/mfa/actions.ts:157`) uses the identical `for update skip locked` atomic pattern but has no dedicated concurrency test of its own — only `security-completeness-item-8.test.ts` (source-inspection) references it. Rate-limited via `mfa_recovery_code_verify` purpose. Excluded from routine MFA (recovery is a separate `pendingAccount('recovery-entry')` stage, never satisfies `decideMfa`'s `challenge-required`). | `lib/auth/mfa/recovery.ts:16-43` (`generateRecoveryCode` → `randomBytes(16)`, 128-bit; `digestRecoveryCode` HMAC-SHA256, one-way); consumption `lib/auth/mfa/recovery-security.ts:9-35` (production) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `idoc.mfa_recovery_codes` (`digest` column only, no plaintext; unique per user+app+digest) | `tests/mfa-store.integration.ts:146-171` (real-Postgres concurrency race, but on `store.consumeRecoveryCode`, not the production `consumeRecoveryCodeWithEvidence`); `tests/canonical-mfa-runtime.test.ts` (unit, entropy/normalization) | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | high | conditional | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 2 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-RECOVERY-005 | Keep recovery authority constrained until authenticator replacement and acknowledgement complete. | implemented-but-unverified | Code path confirmed by direct reading: a consumed recovery code only advances `stage` to `'replacement'`, requiring a fresh TOTP-factor proof + an explicit `acknowledgeRecoveryCodes` (`'recovery-ack'`→`setSession`) step before any session is created. No e2e/integration test exercises this; it is verified only by manual code reading plus source inspection. | `app/(login)/mfa/actions.ts:145-171,198-226` (`beginAuthenticatorRecovery`→`authorizeAuthenticatorRecovery`→`confirmTotpEnrollment` stage `'replacement'`→`'recovery-ack'`); `lib/auth/mfa/pending-primary-auth.ts` (signed, 10-min TTL, stage-typed cookie) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | Transient — JWT cookie `idoc_pending_primary_mfa`, no DB row for the authority itself | `tests/security-completeness-item-8.test.ts` source-inspects that no session is created before replacement/acknowledgement; no behavioral end-to-end recovery test exists. | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | high | conditional | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 2 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-RECOVERY-006 | Permit secure recovery-code regeneration and atomically invalidate the prior set. | partial | Atomic invalidate+replace genuinely happens, but **only as a side effect of TOTP enrollment/replacement** — there is no standalone "regenerate recovery codes" action. `SensitiveAction: 'generate-recovery-codes'` is declared in `types.ts:11` but has zero call sites anywhere (`grep` confirms). The security UI explicitly states "New codes are issued when you replace your authenticator" (`security-client.tsx:54`), and replacing an authenticator itself requires first burning a recovery code (`beginAuthenticatorReplacement`, `dashboard/security/actions.ts:88-98`) — so a member who has *not* lost their device cannot get a fresh code set without an unnecessary detour through the loss-recovery flow. | `lib/auth/mfa/enrollment-finalization.ts:59-65`, `lib/auth/mfa/replacement-finalization.ts:76-83` (`delete` then re-`insert` recovery codes in the same transaction as factor activation) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `idoc.mfa_recovery_codes` | `tests/security-completeness-item-8.test.ts` (source-inspection) | Existing coverage cannot prove the named absent/incomplete mandatory property. | CODE+TEST | high | conditional | Implement the missing property and prove it through the real production path. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 2 | Production database/provider/configuration where named; otherwise existing test harness. | yes | no |
| AUTH-LIFECYCLE-003 | Replace authenticators atomically while preserving authority and invalidating dependent credentials. | implemented-but-unverified | Code review confirms the transaction is correct (old factor locked and re-checked via `for update`, optimistic `session_version` concurrency check at L85-89 handles a concurrent old-factor change by failing the whole transaction). No behavioral proof. | `lib/auth/mfa/replacement-finalization.ts:10-105` (production): preserves old factor active until same-transaction activation of new factor; rotates recovery codes, revokes remembered devices tied to old factor, bumps `session_version`, revokes all `auth_sessions`, writes audit + notification | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `idoc.mfa_factors`, `idoc.mfa_recovery_codes`, `idoc.mfa_remembered_devices`, `idoc.auth_sessions`, `idoc.users.session_version`, `idoc.audit_log`, `idoc.auth_security_notification_outbox` | `tests/security-completeness-item-8.test.ts`, `tests/live-privileged-mfa.test.ts` (source-inspection only); **no** integration/e2e test drives `finalizeAuthenticatorReplacement` itself — `tests/security-e2e/global-setup.ts:58-63` seeds an already-active TOTP factor directly via SQL, bypassing enrollment/replacement entirely, so no e2e test ever executes this function | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | high | conditional | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 2 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-API-003 | Return only safe MFA metadata and never secret factor material. | implemented-but-unverified | Code review is clean; no behavioral test actually renders the page and inspects the real HTTP response for leaked fields. | `app/(dashboard)/dashboard/security/page.tsx:16-27` (only `totpConfigured: Boolean(factor)` boolean + safe passkey subset `{createdAtMs, credentialId, deviceName, lastUsedAtMs}` sent to client); no `app/api/*` route touches `mfaFactors`/`encryptedSecret`/`factorId` (`grep` confirms zero matches) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | N/A | `tests/account-security-management.test.ts:31-36` (source-inspection, `FORBIDDEN_RENDERED_SECRETS` regex against client component) | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | medium | defense-in-depth | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 6 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-CRYPTO-003 | Exclude MFA secrets, codes, and tokens from logs, audit, notifications, and telemetry. | implemented-but-unverified | No logging statements to leak from, and no telemetry/analytics wiring visible in the MFA lib, but the specific "MFA event outbox rows are secret-free" claim is unverified behaviorally for MFA kinds. | No `console.*` calls anywhere under `lib/auth/mfa/` (`grep` confirms); secrets/tokens/codes are only ever passed as function parameters, never serialized to a log sink | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | N/A | `tests/auth-security-events.integration.ts` (real-Postgres, but only exercises `password_changed`/`verified_email_changed` kinds — MFA-specific kinds `authenticator_enrolled`/`authenticator_replaced`/`recovery_code_used`/`passkey_registered`/`passkey_removed` are never behaviorally checked for secret-exclusion, only asserted via source regex in `security-completeness-item-8.test.ts`) | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | medium | defense-in-depth | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 5 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-TRANSACTION-009 | Serialize and atomically bind MFA enrollment, challenge, replacement, and recovery mutations. | implemented-but-unverified | The concurrency *pattern* (row lock + re-check under lock) is identical between the tested store path and the untested production path, giving reasonable confidence, but this is inference, not proof — a real concurrency bug in the production-only files (e.g. lock-ordering deadlock, an unlocked read) would not be caught by any current test. | `lib/auth/mfa/enrollment-finalization.ts`, `replacement-finalization.ts`, `recovery-security.ts` — all wrap `client.begin(async (tx) => ...)` with `for update` locks and re-validated bindings | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `idoc.mfa_enrollment_transactions`, `idoc.mfa_challenge_transactions`, `idoc.mfa_factors`, `idoc.mfa_recovery_codes` | `tests/mfa-store.integration.ts` (real-Postgres concurrency races, but on the parallel unused store functions, not these production files); production files only have source-inspection coverage | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | medium | defense-in-depth | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 2 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-RATE-006 | Apply atomic, purpose-specific rate limits to all MFA verification and recovery paths. | implemented-but-unverified | The underlying rate-limit primitive is well-proven behaviorally; the MFA-specific purpose wiring itself is only visible via source reading of `mfa/actions.ts`, not exercised end-to-end. Identifier is the numeric `userId`, not attacker-controllable factor metadata, so no inventory-leak vector is evident. | `app/(login)/mfa/actions.ts` (`allowed()` helper, purposes `mfa_login_verify`/`mfa_step_up_verify`/`mfa_recovery_code_verify`/`mfa_enrollment_confirm`) layered on top of `idoc.mfa_challenge_transactions.attempt_count`/`max_attempts` | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `idoc.account_request_limits` (dual email+origin buckets, `lib/security/rate-limit.ts:17-45`) | `tests/rate-limit-normalization.integration.ts` (real-Postgres, generic `checkRateLimit` mechanism proven dual-bucket/atomic/secret-free) — but exercises the mechanism with arbitrary purpose strings, not the actual `mfa_*` purposes | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | medium | defense-in-depth | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 2 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-OPERATIONS-004 | Emit secret-free, actionable MFA security events and notifications. | implemented-but-unverified | Mechanism is behaviorally sound and secret-free by design (dedicated `AuthSecurityKind` enum, no arbitrary payload). No distinct "replay detected" notification kind exists (only generic MFA success/failure kinds) — replay attempts surface only as ordinary failed-verification responses, not a dedicated security event. | `lib/notifications/auth-security-events.ts` (`enqueueAuthSecurityNotification`, kinds include `authenticator_enrolled`, `authenticator_replaced`, `recovery_code_used`, `passkey_registered`, `passkey_removed`); calls embedded transactionally in `enrollment-finalization.ts:69-71`, `replacement-finalization.ts:98-101`, `recovery-security.ts:30-32`, `dashboard/security/actions.ts:141-142,157-158` | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `idoc.auth_security_notification_outbox`, `idoc.audit_log` | `tests/auth-security-events.integration.ts` (real-Postgres, generic mechanism: dedupe, secret-free, recipient-owned — but exercised only with non-MFA kinds); MFA-specific call sites are source-inspection only (`security-completeness-item-8.test.ts`) | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | low | operational | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 7 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-IDENTITY-005 | Require fresh authenticated evidence and collision-safe atomic external-identity linking. | implemented-but-unverified | Email-equality-alone auto-link is explicitly rejected (`GoogleAccountLinkRequiredError`) and is verified behaviorally in `google-account-linking.integration.ts`/e2e; the *authenticated linking* atomic-persistence/collision path itself lacks a real-Postgres behavioral test. | `lib/auth/google-identity-linking.ts` (`atomicLink`/`atomicUnlink`, advisory-lock collision detection, atomic audit+notification); `lib/auth/google-identity-link-evidence.ts` (5-min fresh-evidence cookie); linking gated by password + `requireFreshStepUp('change-security-settings')` in `app/(dashboard)/dashboard/security/actions.ts` | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `idoc.external_identities`, `idoc.audit_log`, `idoc.auth_security_notification_outbox` | `tests/google-oidc-adoption.test.ts` (source-inspection); no dedicated behavioral integration/e2e test drives the full linking mutation through Postgres | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | medium | defense-in-depth | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 3 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-AUTHZ-005 | Enforce authorization at each sensitive server mutation boundary. | implemented-but-unverified | "Forbid tenant grants of Super Admin" is not-applicable (no tenants). Self-elevation is structurally impossible (only an existing Super Admin can call this, and it requires fresh step-up), but no test proves the last-Super-Admin concurrency lock actually holds under contention. | `lib/membership/role-grants.ts:28-107` (`grantApplicationRole`/`revokeApplicationRole`: Super-Admin-only actor, `sessionVersion` increment on target, last-Super-Admin protection via `SELECT ... FOR UPDATE` lock, atomic audit+notification); step-up gate in `app/(dashboard)/admin/members/actions.ts:20-28` (`requireFreshStepUp(actor, 'change-privileged-permissions', ...)`) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `idoc.application_roles`, `idoc.audit_log`, `idoc.auth_security_notification_outbox` | No dedicated behavioral test found exercising `grantApplicationRole`/`revokeApplicationRole` against real Postgres (concurrency/last-Super-Admin protection unverified by test) | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | high | direct | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 3 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-STEPUP-003 | Require and behaviorally enforce fresh MFA step-up for sensitive actions. | implemented-but-unverified | Bound to exact action/session/user/version/role, single-use (`consumeStepUpAuthority`), remembered devices never satisfy it (`configuredFactor` ignores `rememberTotpDevice`). No automated integration/e2e test found driving `requireFreshStepUp` end-to-end against real Postgres. | `lib/auth/mfa/step-up.ts:82-125` (`requireFreshStepUp`) and `lib/auth/mfa/decision.ts:24-34` (`sensitiveActionRequiresFreshStepUp`) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `idoc.mfa_challenge_transactions` (purpose=`step-up`), signed `idoc_fresh_step_up`/`idoc_pending_step_up` cookies (not DB-persisted authority) | `tests/live-privileged-mfa.test.ts`; docs/05 §15.5 operator UAT checklist (manual, not automated) | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | high | conditional | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 2 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-OPERATIONS-005 | Fail closed and safely refresh/cache remote identity-provider verification keys. | implemented-but-unverified | Fails closed by construction (any thrown error → `GoogleOidcError`), but no behavioral test simulates a JWKS outage/unknown-key-refresh scenario to prove the failure path actually rejects rather than silently succeeding. | `lib/auth/google-oidc-reference.ts:160-168` (`resolveGoogleJwks`, `jose.createRemoteJWKSet` — bounded built-in key caching/refresh); any JWKS/verification failure throws inside the `try` at `:379-411`, converted to `GoogleOidcError('invalid_id_token')`, never exposing raw provider text (`google_oauth_callback_failed` logs only a category, per `google-oauth-failure-alerting.test.ts`) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | N/A | `tests/google-oauth-failure-alerting.test.ts` (source-inspection: no raw error interpolated into alert/log); no test forces an actual JWKS-unreachable or malformed-key scenario | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | low | operational | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 5 | Production database/provider/configuration where named; otherwise existing test harness. | no | yes |
| AUTH-SECRET-001 | Keep provider client secrets server-only and absent from browser/build artifacts. | implemented-but-unverified | Docs/07 §15.1 documents rotation procedure (client/secret pair via Google console overlap). No automated test scans build output for the secret value (docs/05 "Production build and runtime boundary" describes a scan but it wasn't located/confirmed as an executable test in this pass). | `lib/auth/google-oidc-reference.ts:81-85,149-155` (`GOOGLE_OAUTH_ENV.clientSecret` read server-only via `required()`); never referenced in any client component/`NEXT_PUBLIC_*` var (grep-verified across repo) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | N/A (Vercel env var, not DB) | `tests/google-oidc-adoption.test.ts` (source-inspection: `.env.example` documents the variable name only, no value) | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | medium | defense-in-depth | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 5 | Production database/provider/configuration where named; otherwise existing test harness. | no | yes |
| AUTH-API-004 | Avoid resource-existence and authorization-detail disclosure in API responses. | implemented-but-unverified | Pattern is consistent everywhere reviewed (generic `AuthorizationError` message, no resource-existence leakage), but no behavioral test asserts response bodies/status codes for a real cross-account request at the HTTP layer. | `app/api/user/route.ts` (denial → generic `null`, 200, not error-shaped); `lib/membership/authorization.ts` (`AuthorizationError`, generic message); `lib/membership/data-access.ts:70-74` (`getPrivateMember` returns `null` for both "doesn't exist" and would-be-denied cases before throwing) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | N/A | `tests/authorization-boundary-inventory.test.ts` (source-inspection only) | Current evidence is source inspection, indirect coverage, or a parallel helper; it does not prove the actual production boundary. | TEST | medium | defense-in-depth | A behavioral test invokes the production path and proves the requirement’s positive and adversarial cases. Non-behavioral regex evidence is not sufficient. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 3 | Production database/provider/configuration where named; otherwise existing test harness. | no | no |
| AUTH-LOG-001 | Define and emit the canonical security-event set with sufficient outcome/context. | partial | Correlation/attribution exists; no formal "stable taxonomy" doc of security-event names, tenant field N/A (single-tenant), minimized metadata enforced by convention not a schema. | `lib/observability/logger.ts:11-17` (categorical event+meta), `lib/observability/request-id.ts` (server-generated correlation ID, `middleware.ts` assigns `crypto.randomUUID()`, never from client header) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `idoc.audit_log` (distinct from console logs) | `tests/security-e2e/cookies-and-headers.spec.ts` (proves `x-request-id` server-generated, not client-reflected) | Existing coverage cannot prove the named absent/incomplete mandatory property. | CODE+TEST | medium | defense-in-depth | Implement the missing property and prove it through the real production path. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 6 | Production database/provider/configuration where named; otherwise existing test harness. | yes | no |
| AUTH-LOG-003 | Use structured allowlisted logging that excludes secrets and sensitive personal data. | partial | Categorical/redacted logging confirmed; "retention-classed" and "tenant-isolated" have no explicit implementation since there's a single tenant and no log-retention policy document. | `lib/observability/logger.ts`; `lib/security/password-breach-check.ts:40,50` (documented plain-`console.warn` exception, no request/response body dumps anywhere) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | N/A (logs are ephemeral, not a DB table) | `tests/security-completeness-item-8.test.ts` (source-inspection for categorical-only call sites) | Existing coverage cannot prove the named absent/incomplete mandatory property. | CODE+TEST | medium | defense-in-depth | Implement the missing property and prove it through the real production path. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 6 | Production database/provider/configuration where named; otherwise existing test harness. | yes | no |
| AUTH-OPERATIONS-006 | Classify alerts by severity and correlate repeated/anomalous security events. | partial | The severity-taxonomy half of this requirement is now real and tested. **No correlation/anomaly-detection engine exists**, and this pull request does not attempt one — that remains a genuinely larger, separate capability (pattern/frequency detection across events), not something to claim by proxy. Flagging this half as still missing rather than softening it. | `lib/notifications/breached-password-alert.ts`, `lib/notifications/google-oauth-failure-alert.ts`, `lib/notifications/profile-change-delivery.ts` — each sends a flat email to `IDOC_ADMIN_NOTIFICATION_EMAIL` for its own specific event. `lib/notifications/alert-severity.ts` (added by the alert-severity-taxonomy pull request) now assigns each a fixed informational/warning/high/critical severity, and every alert's subject line is tagged with it (e.g. `[HIGH] IDOC: breached password rejected`) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | N/A | `tests/alert-severity.test.ts` (proves the fixed severity mapping and that every alert call site actually tags its subject, not just that the helper function works in isolation) | Existing coverage cannot prove the named absent/incomplete mandatory property. | CODE+TEST | low | operational | Implement the missing property and prove it through the real production path. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 7 | Production database/provider/configuration where named; otherwise existing test harness. | yes | no |
| AUTH-OPERATIONS-007 | Provide actionable security monitoring coverage and response ownership. | partial | Genuine incident-response runbook section exists (§11) with named actions per scenario, but it is a manual instruction list ("revoke... and escalate"), not a tool/script — an administrator has no dedicated "force-revoke all authority for user X" admin action in code; they'd have to improvise via existing per-user session-revocation primitives. Evidence preservation and incident-correlated audit events are not explicitly wired for this scenario. | Session/device/factor revocation exists (`revokeAllUserSessions`, `forgetAllLoginDevices` in `app/(login)/actions.ts:239-240`) for **self-service** account compromise (password reset, recovery). No code path for **operator-initiated** infrastructure/account compromise response (e.g. an admin-triggered "revoke this user's everything" tool) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `idoc.auth_sessions`, `idoc.login_trusted_devices`, `idoc.mfa_factors` | `tests/account-security-management.test.ts:58-61` (self-service deletion path only) | Existing coverage cannot prove the named absent/incomplete mandatory property. | CODE+TEST | low | operational | Implement the missing property and prove it through the real production path. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 7 | Production database/provider/configuration where named; otherwise existing test harness. | yes | no |
| AUTH-CRYPTO-004 | Inventory cryptographic uses and enforce approved algorithms and parameters. | partial | Non-secret key **version** identification exists (`encryption_key_id` per factor, active-key-ID config). There is no explicit "pending/active/retiring/retired/compromised" lifecycle state machine anywhere — key state is implicit (present-in-ring = usable, absent = fails closed for that key ID), with no distinct "retiring" vs "retired" vs "compromised" semantics and no code to reject a specific key ID as compromised while accepting others. | `lib/db/schema.ts:453` (`mfa_factors.encryption_key_id`), `lib/runtime/configuration.ts:92-107` (key ring as a Map, active-vs-known-but-inactive keys) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `idoc.mfa_factors.encryption_key_id` | `tests/runtime-configuration.test.ts` | Existing coverage cannot prove the named absent/incomplete mandatory property. | CODE+TEST | medium | defense-in-depth | Implement the missing property and prove it through the real production path. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 5 | Production database/provider/configuration where named; otherwise existing test harness. | yes | no |
| AUTH-SECRET-004 | Validate production secret strength and separation for every security purpose. | partial | Pepper is explicitly optional in the canonical requirement and genuinely absent here — reasonable to treat as **not-applicable** for the pepper half (never claimed, never implemented, no false "implemented" claim). OAuth client secret rotation relies entirely on Google's provider-side overlap window plus manual operator coordination — no application code tracks a rollback/retirement audit trail for the secret itself, though it is confirmed never exposed to the client. | Pepper: **not implemented** — `lib/auth/password-hash.ts` has no pepper concept at all (Argon2id with per-password salt only). OAuth client secret: `lib/auth/google-oidc-reference.ts:83` — single `GOOGLE_OAUTH_CLIENT_SECRET` env var, no application-level dual-secret/overlap support | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | N/A | NONE | Existing coverage cannot prove the named absent/incomplete mandatory property. | CODE+TEST | high | conditional | Implement the missing property and prove it through the real production path. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 5 | Production database/provider/configuration where named; otherwise existing test harness. | yes | yes |
| AUTH-PRIVACY-001 | Minimize authentication personal data and define purpose and retention. | partial | Access control and purpose-limited export guidance exist and are enforced/tested. "Bounded retention-export" and "excluded from unrestricted analytics" have no concrete implementation found — no retention-expiry job (see AUTH-STORAGE-007) and no analytics-exclusion mechanism was located (no analytics integration was found at all, which arguably satisfies "excluded from unrestricted analytics" by absence, but that's not a designed control). | `lib/membership/data-access.ts` (owner-or-admin checks via `requireAccountAccess`/`requireAdministrator` on every read), `lib/admin/csv.ts` + `app/api/admin/export/*` (admin-only exports) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `idoc.users`, `idoc.profiles`, etc. — single-tenant, so "tenant-isolated" is trivially true | `tests/authorization-boundary-inventory.test.ts` (structural: every Server Action/Route Handler funnels through `requireAccountAccess`) | Existing coverage cannot prove the named absent/incomplete mandatory property. | CODE+TEST | medium | defense-in-depth | Implement the missing property and prove it through the real production path. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 6 | Production database/provider/configuration where named; otherwise existing test harness. | yes | no |
| AUTH-DEPENDENCY-001 | Fail closed with bounded behavior when security dependencies are unavailable. | partial | Each dependency's failure posture is deliberate and mostly documented in code comments (a real strength), but there's no single unified "dependency risk register" doc, and Postgres itself has no explicit degrade/queue strategy beyond "the whole request fails" (arguably correct fail-closed behavior for an authoritative store, but undocumented as an intentional choice). | Postgres: no explicit handling — connection failure throws and requests fail closed (`lib/db/drizzle.ts`, no try/catch/fallback). Email (Mailchimp): `lib/notifications/mailchimp-transactional.ts:23` throws on non-OK, caught by the outbox retry/dead-letter worker. Turnstile: `lib/auth/turnstile.ts:16-50` explicit fail-closed (missing config, provider error, hostname/action mismatch all return `false`). HaveIBeenPwned: `lib/security/password-breach-check.ts:24-55` explicit fail-**open**, with an inline code comment justifying it as defense-in-depth, not primary control. Google OAuth: no explicit fail-open/closed comment found, but token/JWKS validation failures naturally fail the login attempt closed. | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | N/A | `tests/turnstile-contract.test.ts`, `tests/password-breach-check.test.ts` | Existing coverage cannot prove the named absent/incomplete mandatory property. | CODE+TEST | high | conditional | Implement the missing property and prove it through the real production path. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 5 | Production database/provider/configuration where named; otherwise existing test harness. | yes | yes |
| AUTH-OPERATIONS-008 | Prove backup/restore, retention, and security reconciliation operationally. | partial | Cross-instance consistency is achieved architecturally (single Postgres source of truth, no local/in-memory caches for revocation/roles/rate-limits found), but there's no explicit clock-skew monitoring or alerting — reliance is entirely implicit on Postgres's own clock via `now()`. | `idoc.auth_sessions` (single shared Postgres instance — no per-instance state), `idoc.account_request_limits` (shared rate-limit buckets), role checks always re-read from `idoc.application_roles` per request (`lib/membership/data-access.ts`) — cross-instance consistency follows from a single authoritative database with no local caching layer found | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | `idoc.auth_sessions`, `idoc.application_roles`, `idoc.account_request_limits` | Not specifically tested for multi-instance race conditions beyond the general test suite | Existing coverage cannot prove the named absent/incomplete mandatory property. | OPERATIONS | low | operational | Implement the missing property and prove it through the real production path. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 8 | Production database/provider/configuration where named; otherwise existing test harness. | no | yes |
| AUTH-OPERATIONS-010 | Maintain deploy-time security checks and production validation evidence. | partial | Dependency install, typecheck, unit/integration tests, and build are all real, enforced CI gates on every PR. **No whitespace/lint check and no "contract documentation change" validation step were found** — no ESLint/Prettier config referenced in CI, no doc-drift check verifying docs/21 or docs/05 stay in sync with code changes. This is a genuine partial gap, not "verified." | `.github/workflows/auth-security-verification.yml` (install → `pnpm audit` → `pnpm typecheck` → `pnpm test:ci` → Playwright security e2e), `.github/workflows/release-1-verification.yml` (install → `pnpm check:release1` = typecheck+test+build) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | N/A | The workflows themselves are the test evidence | Existing coverage cannot prove the named absent/incomplete mandatory property. | OPERATIONS | low | operational | Implement the missing property and prove it through the real production path. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 8 | Production database/provider/configuration where named; otherwise existing test harness. | no | yes |
| AUTH-OPERATIONS-011 | Maintain incident-ready operational procedures and execute required drills. | partial | The checklist exists and is explicitly non-self-certifying (a real strength — it doesn't silently claim security), but it's a markdown document requiring a human to tick boxes, not a machine-readable evidence artifact (e.g. no JSON/YAML manifest, no CI step that fails a PR if the checklist itself is stale). | **NOT FOUND** as a distinct machine-readable checklist — the closest artifacts are docs/07 §15.6 "Release signoff (leave unchecked until manually proved)" (a markdown checkbox list) and docs/11 (release-1-verification-matrix.md, not opened in this pass) | Production route/action importing or invoking the named implementation; close with a call-path test rather than a parallel helper. | N/A | The `check:release1`/`test:security` CI gates are automated *tests*, not a readiness *checklist* | Existing coverage cannot prove the named absent/incomplete mandatory property. | OPERATIONS | low | operational | Implement the missing property and prove it through the real production path. | Use the production route/action/function with real Postgres, signed cookies/JWTs, or Chromium as technically appropriate; assert success, denial, persistence, and concurrency/expiry boundaries. | Slice 8 | Production database/provider/configuration where named; otherwise existing test harness. | no | yes |

## Ordered remediation slices

1. **Session, MFA, and recovery integrity — AUTH-SESSION-007, AUTH-COOKIE-002, AUTH-MFA-005,
   AUTH-RECOVERY-004, AUTH-RECOVERY-005, AUTH-RECOVERY-006, AUTH-LIFECYCLE-003,
   AUTH-TRANSACTION-009, AUTH-RATE-006, AUTH-STEPUP-003.** TEST plus CODE+TEST for recovery-code
   regeneration. Likely files: session registry/UI, MFA actions/finalizers, recovery security, e2e and
   Postgres integration tests. Migration: no expected. External provider: no. Acceptance: production
   functions prove atomicity, replay/concurrency denial, expiry, regeneration, cookie contract, and fresh
   step-up.
2. **Authorization and privilege — AUTH-STORAGE-002, AUTH-LIFECYCLE-002, AUTH-PASSWORD-005,
   AUTH-IDENTITY-005, AUTH-AUTHZ-005, AUTH-API-004.** TEST. Likely files: authorization/data-access,
   role/link/password actions, HTTP/e2e tests. Migration: no. External provider: no. Acceptance: direct
   cross-account and lifecycle-state requests cannot mutate or distinguish protected resources.
3. **CSRF/request integrity — AUTH-CSRF-003.** CODE+TEST. Likely files: middleware, form/action token
   helper, relevant forms, Chromium CSRF suite. Migration: no. External provider: no. Acceptance:
   unpredictable purpose/session-bound tokens are checked, expire/rotate, and reject missing, replayed,
   and cross-session submissions while Origin validation remains defense in depth.
4. **Credential and key lifecycle — AUTH-STORAGE-005, AUTH-STORAGE-006, AUTH-CRYPTO-003,
   AUTH-OPERATIONS-005, AUTH-SECRET-001, AUTH-CRYPTO-004,
   AUTH-SECRET-004, AUTH-DEPENDENCY-001.** TEST and CODE+TEST. Likely files: password hash, MFA crypto,
   OIDC/JWKS, runtime configuration, logging, operations docs/tests. Migration: possible if key-ring
   metadata is persisted; otherwise no. External provider: JWKS outage/provider validation only.
   Acceptance: rotation overlap/retirement/compromise, secret separation/strength, build-output exclusion,
   and dependency fail-closed behavior are behaviorally demonstrated.
5. **Privacy and logging — AUTH-IDENTITY-003, AUTH-API-003, AUTH-LOG-001, AUTH-LOG-003,
   AUTH-PRIVACY-001.** CODE+TEST. Likely files: normalization, security page response, logger/event schema,
   retention docs/tests. Migration: possible for preserved display email/retention changes. External
   provider: no. Acceptance: normalized identity semantics, minimized responses, structured allowlisted
   security events, and enforceable retention are proven.
6. **Operations and observability — AUTH-ERROR-001, AUTH-EMAIL-002, AUTH-OPERATIONS-004,
   AUTH-OPERATIONS-006, AUTH-OPERATIONS-007.** TEST and CODE+TEST. Likely files: auth responses, email OTP,
   notification alerts, operational tests/docs. Migration: possible for correlation state. External
   provider: no for repository closure. Acceptance: stable generic responses, resend boundaries,
   secret-free events, severity, correlation, ownership, and response procedure are demonstrated.
7. **External/manual deployment validation — AUTH-OPERATIONS-008, AUTH-OPERATIONS-010,
   AUTH-OPERATIONS-011.** OPERATIONS/EXTERNAL-MANUAL. Likely files: docs/07 evidence record and deployment
   checklist only. Migration: no. External provider: yes (Render, Vercel, Mandrill/Mailchimp). Acceptance:
   dated operator evidence records the restore drill, deployed configuration validation, provider test,
   reconciliation, and incident exercise; repository assertions alone cannot close these items.

## External and operational evidence register

These items remain separate from repository-automated verification:

- **Production residual data:** run read-only `select count(*) from idoc.team_members` and `select
  count(*) from idoc.activity_logs where ip_address is not null`. Non-zero results require a retention,
  purpose, and notification assessment. Repository removal of writers cannot prove production row counts.
- **Render restore drill:** docs/07 documents the procedure, but no evidence establishes that a real restore,
  authentication-key rotation, and suspension/deletion/role reconciliation drill has occurred.
- **Mandrill webhook:** repository tests prove IDOC signature and route behavior; a dated live Mandrill test
  webhook is still provider/deployment evidence.
- **Production configuration:** validate—not infer—`SUPPORT_EMAIL`,
  `MAILCHIMP_TRANSACTIONAL_WEBHOOK_KEY`, remembered-TOTP enablement/days/digest key, `AUTH_SECRET`, MFA
  encryption keys, cron secrets, Google OIDC values, and administrator notification destinations in the
  deployed environment. Never record secret values in evidence.

## Maintenance rule

Update documents 22 and 23 atomically whenever a control changes status. Recount unique expanded IDs and
assert that `verified + implemented-but-unverified + partial + missing + not-applicable = 155` and that
this backlog contains exactly the same canonical IDs and classifications as the applicable
`implemented-but-unverified + partial + missing` rows in document 22. Historical changelog prose may
remain only when explicitly labeled historical.
