# 22. Canonical Auth Contract Evidence Matrix

## Purpose and scope

This document is the current, from-scratch evidence matrix for every requirement in the canonical
`pixelsmith-auth-reference` machine contract (`src/contract/requirements.ts`), evaluated against the
actual idoc.club codebase as of this revision. It supersedes any prior evidence-matrix claims in
`docs/21-authentication-security-control-inventory.md` as the authoritative status source; docs/21
retains its own descriptive sections (cookie attribute table, CSRF/rate/header/Turnstile/config/log/DB/
supply-chain notes, and its changelog of individual pull requests) and is not being deleted, but its
per-requirement status claims should be read as historical narrative, not current status — this
document is current status.

**Canonical versions confirmed at the time of this audit** (matching `pixelsmith-auth-reference` `main`
exactly, no drift): contractVersion 2.0.0, machine schemaVersion 14.0.0, validatorVersion 11.0.0,
mapping schemaVersion 1.0.0, portable-config schemaVersion 2.0.0.

## Methodology

Five independent evidence-gathering passes were run against the current idoc.club codebase, one per
canonical contract stage (Foundational + Stage 2 session/cookie/CSRF/rate/bot/error/redirect/transport/
API/framework/storage; Stage 3 signup/email/OTP/password; Stage 4 MFA/TOTP/recovery/remembered-device;
Stage 5 OAuth/invite/tenant/authorization; Stage 6 log/audit/operations/crypto-lifecycle/privacy/
dependency-failure). Each pass traced the actual trusted-server call chain for every requirement in
scope — reading the implementation, its persistence layer, and its test evidence directly — rather than
inferring behavior from file or function names. IDOC's own `docs/21` inventory uses an independent local
ID-numbering scheme that only coincidentally matches canonical IDs at low numbers; this matrix is keyed
to the canonical IDs directly, cross-referencing docs/21's local IDs only where explicitly noted.

### Assessment legend

- **verified** — real behavioral evidence exists (a test that executes the actual production code path
  against real infrastructure — Postgres, a real browser, a signed JWT — and observes the result), not
  merely source inspection.
- **implemented-but-unverified** — the implementation is present and reads correctly on direct code
  inspection, but no behavioral test exercises the actual production code path; existing test coverage
  (if any) is source-inspection-only (regex assertions against file text) or exercises a different,
  parallel code path than the one production actually calls.
- **partial** — a real, specific gap exists in an otherwise-implemented control. The gap is named
  explicitly in the Notes column, not glossed over.
- **missing** — no implementation exists for a requirement that applies to IDOC's product model.
- **not-applicable** — the requirement does not apply given IDOC's confirmed product model (single-
  tenant, single-application; TOTP is compulsory-by-role rather than optional-per-user; a given feature
  is implemented in code but has zero production callers and is therefore inert), with rationale stated.

### IDOC product model, as confirmed by this audit

IDOC is a **single-tenant, single-application platform**: one Vercel deployment, one Postgres schema,
no organization/tenant entity anywhere in the schema used as an authorization boundary (`memberships`
records club-entitlement status, not organizational membership). The `invitations` table and
`ActivityType.INVITE_TEAM_MEMBER`/`'invite-privileged-user'` are genuinely unreferenced Vercel
SaaS-starter-template leftovers (`grep`-confirmed zero call sites outside schema/type definitions). There
is no invitation flow; Administrator/Super Admin accounts are provisioned by an existing Super Admin
directly calling `grantApplicationRole` (`lib/membership/role-grants.ts`), a documented deliberate policy
(docs/05 §4, docs/07 §11), not an omitted feature. This makes AUTH-INVITE-001/002/003 correctly
not-applicable. **The `teams`/`team_members` tables were a separate case, now fixed**: `app/layout.tsx`
called `getTeamForUser()` on every request as a live (if unpopulated) SWR-fallback query — a Codex review
on the PR that introduced this document correctly caught an earlier draft's claim that this was dead
code, and AUTH-TENANT-001/002 was marked `partial` rather than `not-applicable` on that basis. A later
pull request removed the call site and the now-fully-dead query functions it was the only caller of;
AUTH-TENANT-001/002 is `not-applicable` again, see the Stage 5 detail section below.

Separately, this audit found that IDOC's TOTP "remembered device" subsystem
(`lib/auth/mfa/remembered-device.ts`, table `idoc.mfa_remembered_devices`) is fully implemented and
integration-tested but has **zero production callers** — the only production caller of `decideMfa`
(`lib/auth/mfa/login.ts`) hardcodes `rememberTotpDevice: false`. AUTH-REMEMBER-002/003/004 are marked
not-applicable on that basis (the feature is not offered to users, not that a required control within an
offered feature is missing) rather than missing/partial, but this is flagged for a product decision:
either wire the feature up, or remove the dead code, per docs/21 §8's own conventions around not
retaining stale/inert claims. AUTH-REMEMBER-001 (the policy-config premise itself) is marked missing
rather than not-applicable, since a config surface for it is a prerequisite either way.

## Summary counts

Across the 155 canonical requirement IDs actually represented in the rows below (several rows bundle
more than one canonical ID that share a single status — the Turnstile/bot-defense IDs, and the
invitation/tenant not-applicable groups — expanded here to a per-ID count rather than a per-row count,
per a Codex review correction on this PR that caught the original per-row tally undercounting by 12 IDs),
current status breaks down as:

| Status | ID count | Share |
|---|---|---|
| verified | 101 | 65.2% |
| implemented-but-unverified | 28 | 18.1% |
| partial | 18 | 11.6% |
| missing | 1 | 0.6% |
| not-applicable | 7 | 4.5% |

(This table also reflects several corrections made after the initial recount: AUTH-TENANT-001/002 moved
from not-applicable to partial (a Codex review on the pull request that introduced this document caught a
live-but-unpopulated read path an earlier draft had wrongly called dead code) and then back to
not-applicable (a later pull request removed that path); AUTH-PRIVACY-002 moved from verified to partial
(the same review pass caught an unassessed raw-IP-capable schema column) and then to verified (the same
later pull request removed its read/render path); AUTH-LIFECYCLE-006 moved from partial to verified in
another later pull request that implemented admin-initiated account-authentication suspension;
AUTH-RATE-004 moved from partial to verified in a further pull request that gated proxy-header trust on
the actual Vercel deployment topology and added IP-shape validation — see their rows below for detail.)

This reflects a codebase with strong, often real-Postgres/real-browser behavioral coverage for its core
authentication surface (session lifecycle, password storage, MFA challenge/replay defense, OAuth
transaction binding, CSRF, rate limiting, audit immutability), alongside a specific, named set of gaps —
most concentrated in Stage 6 operational maturity (backups, bounce handling, alert taxonomy, purge jobs,
key-rotation completeness) rather than in the core authentication path itself.
**This is not a "production ready" verdict.** It is evidence for scoping the remediation slices that
follow; a full final verdict (limited to "not ready" or "ready for application-specific production
validation") is reserved for the end of the remediation program, after all planned slices are complete
and a fresh audit is run against the final proposed head.

## Most consequential gaps (do not soften)

1. **Unverified residual production data in `idoc.team_members`/`idoc.teams`/`idoc.activity_logs.ip_address`**
   — **operational, not a code gap** (a Codex review on the pull request that removed the corresponding
   dead code paths caught this). The original SaaS-starter template wrote to all three
   (`db.insert(teamMembers)` and `logActivity()`'s `ip_address` field) until commit `a3192f1` removed
   that code, and `docs/08`'s own status line records "raw subscription starter deployed" as a real past
   state, not a hypothetical one. Whether the production database actually holds residual rows from that
   period — potentially real team-member names/emails or real IP addresses — cannot be determined from
   repository state. **Before any production-readiness claim, run a direct read-only query against the
   production database** (`select count(*) from idoc.team_members`, `select count(*) from
   idoc.activity_logs where ip_address is not null`) and, if either returns non-zero, treat the result as
   real user data requiring its own assessment (retention/purpose/notification), not as evidence the code
   fix was insufficient — the code fix is complete and verified independently of this question.
2. **AUTH-STORAGE-008 (backups)** — `missing`. No backup/restore code, script, or documented
   step-by-step procedure exists anywhere in the repository; only a one-line quarterly "check the
   posture" reminder in docs/07 §12.
3. **AUTH-OPERATIONS-006 (alert correlation/anomaly detection)** — `partial`. A real severity
   taxonomy now exists and every existing admin-email alert's subject is tagged with it, but no
   correlation/anomaly-detection engine exists — each alert still fires independently, with no
   pattern or frequency detection across events.
4. **AUTH-CRYPTO-005 / AUTH-SECRET-003 (key rotation completeness)** — `partial`. TOTP secret encryption
   genuinely supports multi-key rotation, but session-key (`AUTH_SECRET`) rotation has no ring/overlap at
   all (documented hard cutover), and there is no distinct "compromised key" state — only full removal,
   which is all-or-nothing and generates no dedicated audit event.
5. **AUTH-CSRF-003 (token-based CSRF)** — `partial`. IDOC relies entirely on Origin-header validation,
    not issued/rotated CSRF tokens; effective in practice but does not literally satisfy this control.

The strongest, most genuinely verified items across all five stages: session-registry replay defense
(AUTH-SESSION-002/009/010, directly proven by dedicated e2e tests including one added by this program's
own legacy-session-removal PR), TOTP/recovery-code replay and concurrency defense (AUTH-MFA-006, passkey
sign-count regression defense), password storage (Argon2id, AUTH-STORAGE-004), OAuth transaction/state/
PKCE binding (AUTH-TRANSACTION-010, AUTH-OAUTH-002), audit-log DB-level immutability via trigger
(AUTH-AUDIT-002), and fail-closed production configuration validation (AUTH-OPERATIONS-009).

---


## Stage: Foundational + Stage 2 — Session, Cookie, CSRF, Rate Limiting, Bot Defense, Error Handling, Redirects, Transport, API, Framework, Storage

| ID | Implementation location | Persistence location | Test evidence | Assessment | Notes |
|---|---|---|---|---|---|
| AUTH-SESSION-001 | `lib/auth/session.ts:52-68` (`getSession` requires server-verified JWT + registry match) | `idoc.auth_sessions` (`lib/auth/session-registry.ts`) | `tests/security-e2e/session-replay.spec.ts` (real browser, JWT-only replay rejected) | verified | No client state establishes auth; registry is authoritative. |
| AUTH-TRANSACTION-001 | `lib/auth/session.ts:70-93` `setSession`; `lib/membership/account-recovery.ts` transactions | `idoc.auth_sessions`, `idoc.account_tokens` | `tests/session-registry-adversarial.integration.ts` | verified | |
| AUTH-AUTHZ-001 | `lib/membership/authorization.ts`; `lib/membership/data-access.ts` `requireAccountAccess` | N/A | `tests/authorization-boundary-inventory.test.ts`, `tests/authorization-matrix.integration.ts` | verified | |
| AUTH-AUTHZ-002 | `lib/auth/mfa/decision.ts` (`role` taken from authoritative DB, not client); `lib/membership/authorization.ts` | N/A | `tests/security-e2e/auth-boundaries.spec.ts` (role/account-state boundaries on direct requests) | verified | Single-tenant product — no client-supplied tenant/org assertions exist to validate. |
| AUTH-FRAMEWORK-001 | `middleware.ts` (redirect-only gate) + every Server Action re-checks via `getSession()`/`requireAccountAccess` | N/A | `tests/security-e2e/auth-boundaries.spec.ts` | verified | Middleware is defense-in-depth, not sole boundary — confirmed by direct-request tests. |
| AUTH-STORAGE-001 | N/A — no `localStorage`/`sessionStorage`/IndexedDB auth usage found (`grep` across `app/`, `components/` returned no matches) | N/A | N/A | verified | Absence confirmed by exhaustive search, not merely assumed. |
| AUTH-CRYPTO-001 | `node:crypto` `randomBytes`/`randomInt`/`randomUUID` used throughout: `lib/auth/email-otp.ts:26`, `lib/auth/mfa/recovery.ts:17`, `lib/auth/mfa/remembered-device.ts:24`, `lib/membership/account-recovery.ts:47`, `lib/auth/session.ts:74` | various (see below) | `tests/canonical-mfa-runtime.test.ts` (recovery-code entropy asserted) | verified | |
| AUTH-TRANSACTION-002 | `lib/auth/turnstile.ts` (action binding), `lib/auth/mfa/types.ts` purpose fields, `lib/auth/pending-password-reset.ts` (stage machine per purpose) | `idoc.mfa_*`, `idoc.account_tokens` | `tests/turnstile-contract.test.ts`, `tests/mfa-store.integration.ts` | verified | |
| AUTH-TRANSACTION-003 | `lib/auth/email-otp.ts:98-116` (single `UPDATE...WHERE consumed_at IS NULL...RETURNING`), `lib/membership/account-recovery.ts:152-192`, `lib/auth/mfa/recovery-security.ts` (`FOR UPDATE SKIP LOCKED`) | `idoc.email_otp_codes`, `idoc.account_tokens`, `idoc.mfa_recovery_codes` | `tests/mfa-store.integration.ts:146` (concurrent recovery-code consumption, one winner) | verified | |
| AUTH-TRANSACTION-004 | Same atomic-consume pattern as above; TOTP counter replay guard `lib/auth/mfa/store.ts`/`types.ts` `acceptTotpChallenge` | `idoc.mfa_totp_factors` | `tests/canonical-mfa-runtime.test.ts:121` (invalid codes charged against bound challenge); `tests/security-e2e/google-oauth.spec.ts:140` (consumed callback can't replay) | verified | |
| AUTH-MFA-001 | `lib/auth/mfa/types.ts` (`TotpFactorRecord`, `WebAuthnCredentialRecord` both modeled as generic factors); `lib/auth/mfa/step-up.ts:26-37` `configuredFactor: 'none'\|'policy-factor'\|'totp'\|'webauthn'` | `idoc.mfa_totp_factors`, `idoc.webauthn_credentials` | `tests/webauthn-mfa-wiring.test.ts` | verified | TOTP and WebAuthn both modeled; not TOTP-only. |
| AUTH-MFA-002 | `lib/auth/mfa/pending-primary-auth.ts` + `lib/auth/mfa/login.ts` `beginPrimaryMfa` — `setSession()` is only ever called after `beginPrimaryMfa` returns false (see `app/(login)/sign-in/actions.ts:100`, `app/api/auth/google/callback/route.ts:99-104`) | `idoc_pending_primary_mfa` cookie (JWT, not DB) | `tests/security-e2e/auth-boundaries.spec.ts:13` ("pending-flow cookies are not interchangeable with a session") | verified | |
| AUTH-RECOVERY-001 | `lib/auth/pending-password-reset.ts` (15-min JWT, distinct cookie/stage machine, never grants dashboard session) | N/A (cookie-only, short-lived) | `tests/security-e2e/auth-boundaries.spec.ts:13` | verified | Recovery state cannot reach `/dashboard`; distinct from `setSession`. |
| AUTH-OPERATIONS-001 | `lib/auth/turnstile.ts` (missing/invalid config → `return false`), `lib/security/rate-limit.ts` (missing secret throws), `lib/auth/mfa/recovery.ts:27` (short secret throws) | N/A | `tests/turnstile-contract.test.ts:116,131` (fail-closed on missing secret/BASE_URL, every NODE_ENV) | verified | |
| AUTH-FRAMEWORK-002 | `middleware.ts:34-48` (closes Next.js Server Action same-origin gap for absent `Origin` header) | N/A | `tests/security-e2e/csrf.spec.ts:39,59` | verified | Explicit, documented closing of a Next.js default weakness. |
| AUTH-IDENTITY-001 | `lib/auth/mfa/decision.ts` `decideMfa`/`roleRequiresTotp` (policy config: `TotpRequirement` enum) | N/A | `tests/canonical-mfa-runtime.test.ts:43` | verified | |
| AUTH-STORAGE-002 | All DB writes gated behind `'server-only'` modules with authorization/purpose checks first (e.g. `lib/membership/account-recovery.ts`, `lib/auth/session-registry.ts`) | multiple tables | integration suite broadly | implemented-but-unverified | No single test proves *every* write path; architecturally consistent but not exhaustively tested as one property. |
| AUTH-SESSION-002 | `lib/auth/session.ts:40-50` `registeredSessionIsValid` (fails closed on absent/mismatched registry row); `lib/auth/session-registry.ts:38-57` `readActiveSession` (excludes revoked/expired) | `idoc.auth_sessions` | `tests/security-e2e/session-replay.spec.ts` (5 tests: unregistered JWT, version mismatch, direct revocation, legacy shape, positive control) | verified | |
| AUTH-SESSION-003 | `lib/auth/session-tokens.ts:74` `randomUUID()` sessionId; cookie never in URL; `lib/observability/logger.ts` never logs cookie values | `idoc.auth_sessions` | `tests/session-token-boundaries.test.ts` | verified | |
| AUTH-SESSION-004 | `lib/auth/session.ts:70-93` `setSession` mints new `sessionId`/registry row on every auth event (login, google, MFA, recovery); `sessionVersion` bump invalidates prior tokens on password reset/migration activation (`lib/membership/account-recovery.ts:120,184`) | `idoc.auth_sessions`, `idoc.users.session_version` | `tests/role-grant-session-invalidation.test.ts` | verified | |
| AUTH-SESSION-005 (policy) | `lib/auth/session-tokens.ts:14-15` `SESSION_IDLE_SECONDS=1800`, `SESSION_ABSOLUTE_SECONDS=43200`; `lib/auth/mfa/step-up.ts:21` step-up TTL 5 min; `lib/auth/mfa/remembered-device.ts:17` 1-90 days | N/A | `tests/session-token-boundaries.test.ts` (boundary math) | verified | |
| AUTH-SESSION-006 | `lib/auth/session.ts:95-115` `clearSession` (revokes registry before clearing cookie); `lib/auth/session-registry.ts:78-114` `revokeAllUserSessions`/`revokeOtherUserSessionsWithEvidence` | `idoc.auth_sessions.revoked_at/revoke_reason` | `tests/security-e2e/sessions.spec.ts:3` ("logout revokes registry session so copied cookie can't be replayed") | verified | |
| AUTH-SESSION-007 (policy) | `lib/auth/session-registry.ts:116-134` `listActiveSessions` (per-session revocability by `sessionId`) | `idoc.auth_sessions` | integration coverage only (no e2e for multi-session UI found beyond `revokeOtherUserSessionsWithEvidence`) | implemented-but-unverified | |
| AUTH-COOKIE-001 | `lib/auth/session-tokens.ts:41-48` `canonicalCookieSecurityAttributes` (httpOnly, sameSite=lax, secure in prod, path=/) | N/A | `tests/security-e2e/cookies-and-headers.spec.ts:49` | verified | |
| AUTH-COOKIE-002 | `lib/auth/session-tokens.ts:16` `PRODUCTION_SESSION_COOKIE_NAME='__Host-idoc-session'`, no Domain set, `path:'/'` | N/A | `tests/security-e2e/cookies-and-headers.spec.ts:49` (dev-mode contract only, per test title) | implemented-but-unverified | `__Host-` prefix only takes effect in production; the e2e test explicitly covers the *development* cookie name, not the `__Host-` production variant — production behavior is code-verified but not e2e-proven in this suite. |
| AUTH-COOKIE-003 | `sameSite:'lax'` used throughout (session, pending-MFA, pending-reset, Google OAuth binding cookies), paired with Origin-based CSRF check in `middleware.ts` | N/A | `tests/security-e2e/csrf.spec.ts` | verified | Lax is deliberate and paired with server-side origin validation, not relied on alone. |
| AUTH-COOKIE-004 | `lib/auth/session-tokens.ts:57-63` `expiredSessionCookieOptions` mirrors exact name/path/attributes of the live cookie | N/A | `tests/security-e2e/sessions.spec.ts` | verified | |
| AUTH-TRANSACTION-005 | `lib/auth/pending-password-reset.ts`, `lib/auth/mfa/pending-primary-auth.ts`, `lib/auth/google-oidc-reference.ts` transaction records (all bind subject, purpose, expiry, transactionId) | `idoc.google_oidc_transactions` (per `0017_google_oidc.sql`), MFA cookies (JWT, not DB) | `tests/google-oidc-transactions.integration.ts` | verified | |
| AUTH-TRANSACTION-006 | `lib/auth/email-otp.ts:106-109` (`attemptCount` recorded before consumption is attempted) | `idoc.email_otp_codes` | `tests/mfa-store.integration.ts:113` | verified | |
| AUTH-TRANSACTION-007 | `lib/auth/google-oidc-reference.ts:214-227` `normalizeReturnTo` rejects any client-supplied path/origin that doesn't match the trusted application origin | N/A | `tests/security-e2e/google-oauth.spec.ts:112` | verified | |
| AUTH-CSRF-001 | Every mutation is a Next.js Server Action; `middleware.ts:34-48` closes the no-Origin gap | N/A | `tests/security-e2e/csrf.spec.ts` (4 real HTTP behavioral tests, including forged-Origin sign-out attempt) | verified | |
| AUTH-CSRF-002 | GET routes are read-only except OAuth `start`/`callback`, which are protected by the HMAC binding cookie (`lib/auth/google-oauth-browser-binding.ts`) rather than being unprotected state changes | N/A | `tests/security-e2e/google-oauth.spec.ts:112` (missing/tampered binding rejected) | verified | |
| AUTH-CSRF-003 | Not implemented as classic CSRF tokens — IDOC relies entirely on Origin-header validation (`middleware.ts` + Next.js's built-in Server Action check), not on issued/rotated CSRF tokens | N/A | `tests/security-e2e/csrf.spec.ts` | partial | The requirement specifically describes token-based CSRF defense (unpredictable, server-validated, expiring/rotating tokens); IDOC's Origin-check mechanism is a legitimate alternative but does not literally satisfy "tokens…expired/rotated." |
| AUTH-CSRF-004 | `lib/auth/google-oauth-browser-binding.ts` (HMAC state binding, `timingSafeEqual`); `lib/auth/google-oidc-reference.ts` validates `state`/nonce server-side | N/A | `tests/security-e2e/google-oauth.spec.ts:112,140` | verified | |
| AUTH-RATE-001 | `lib/security/rate-limit.ts:37-49` `checkRateLimit` (dual bucket: email + origin, keyed by `purpose`) | `idoc.account_request_limits` | `tests/rate-limit-normalization.integration.ts` (7 real-Postgres tests) | verified | |
| AUTH-RATE-002 | `lib/security/rate-limit.ts:9-13` fixed windows/max-requests constants. **Correction:** an earlier draft of this row claimed no explicit `Retry-After` header emission exists anywhere; `app/api/address/autocomplete/route.ts:37-40` — the codebase's one genuine HTTP 429 response point, gated by the same `checkProviderRateLimit` this row already cites — already sets `Retry-After: '900'` (matching `WINDOW_MS`), and had done so since before this audit. The two OAuth entry routes (`app/api/auth/google/{start,link/start}/route.ts`) fail via a 302 redirect to a failure page, not a 429, so `Retry-After` does not apply there; Server-Action-driven cooldowns (email OTP, password reset issuance) are an RPC mechanism with no HTTP status/header control, and correctly surface the same wait duration in-band instead (`lib/auth/email-otp.ts:55` `retryAfterMs`) — the transport-appropriate equivalent, not a gap | `idoc.account_request_limits` | `tests/rate-limit-normalization.integration.ts`; `tests/authorization-boundary-inventory.test.ts:216-217` directly asserts both `status: 429` and `Retry-After` on the autocomplete route | verified | Burst/sustained bounded windows exist and are verified; the one HTTP-status-bearing 429 response in the codebase correctly emits `Retry-After`, and every other rate-limited path (redirect or Server Action) has no HTTP status code to attach the header to in the first place. |
| AUTH-RATE-003 | `lib/security/response-timing.ts` (`equalizeAnonymousResponse`, constant ≥350ms + jitter); `lib/membership/account-recovery.ts` returns identical response regardless of account existence | N/A | `tests/response-timing.test.ts` | verified | |
| AUTH-RATE-004 | **Fixed in a later pull request; hardened per a Codex review finding on that PR.** `lib/security/request-origin.ts` `resolveRequestOrigin()` (pure logic) + `lib/security/rate-limit.ts` `requestOrigin()` (thin `next/headers` wrapper): X-Forwarded-For/X-Real-IP are now trusted only when `process.env.VERCEL` is set (Vercel's edge network is this application's sole documented deployment topology and the only hop that sets these headers itself); outside that, always `'unknown'`. Even when trusted, the resolved value must match a plausible IPv4/IPv6 shape or it also collapses to `'unknown'` — a garbage or attacker-injected string is never used verbatim as a rate-limit bucket identifier. **Codex review finding, fixed:** `requestPasswordRecovery`/`requestMigrationActivation` (`app/(login)/actions.ts`) each carried their own inline copy of the exact pre-fix logic, bypassing `requestOrigin()` entirely even though `checkRateLimit`'s origin-keyed bucket depends on it — an attacker could still rotate `X-Forwarded-For` to bypass the per-origin limit on these two specific flows regardless of the fix above. Both now call `requestOrigin()`. | N/A | `tests/trusted-proxy-headers.test.ts` (10 unit tests: 9 on `resolveRequestOrigin()` itself — not-on-Vercel always unknown regardless of header presence/well-formedness; on-Vercel valid IPv4/IPv6 accepted; multi-hop XFF takes the first entry; X-Real-IP fallback; out-of-range-octet and non-IP-shaped values rejected; an invalid XFF entry fails safe rather than falling through to X-Real-IP — plus 1 source-inspection test proving no call site in `app/(login)/actions.ts` reads the raw headers directly any longer) | verified | The Vercel-detection branch itself cannot be exercised against a literal production Vercel runtime from this repository (the same inherent limit as every other `env.VERCEL`-gated branch in this codebase, e.g. `resolveGoogleOidcProvider`); the security-relevant validation logic — never trusting these headers unless that flag is set, and always validating IP shape — is fully and directly unit-tested, and it is now the *only* origin-derivation path in this codebase (`grep`-confirmed). |
| AUTH-BOT-001..009 (layered defense, fail-closed, flow-entry, Siteverify binding, single-use, defense-in-depth, no client booleans, key separation, presentation non-authoritative) | `lib/auth/turnstile.ts` (server verify, hostname+action binding, fail-closed); `components/turnstile-widget.tsx` (client presentation only, `NEXT_PUBLIC_TURNSTILE_SITE_KEY` public key, secret server-only); wired at signup/login/password-reset entry (`app/(login)/*/actions.ts`) | N/A (Cloudflare-side single-use enforcement, not app-tracked) | `tests/turnstile-contract.test.ts` (12 tests: hostname/action binding, fail-closed on missing config/network/malformed JSON/non-2xx, no NODE_ENV bypass); `tests/turnstile-widget-resilience.test.ts` | verified (for 001,002,004,005,006,007,008,009); implemented-but-unverified (003) | AUTH-BOT-003 ("provider fail-closed behavior") is proven; single-use-token enforcement (part of 005) relies on Cloudflare's own one-time-token semantics — not independently verified against replay in this codebase's tests. |
| AUTH-ERROR-001 | Generic messages for persistent failures, e.g. `app/(login)/sign-in/actions.ts:74` "We could not finish signing you in automatically. Contact IDOC for help." | N/A | none found (no test asserts error-message class taxonomy) | implemented-but-unverified | |
| AUTH-ERROR-002 | No stack/SQL/exception text ever returned to client; `lib/observability/logger.ts` + `deliveryFailureCategory`/`operationalFailureCategory` helpers deliberately discard exception text before logging | N/A | `tests/evidence-safety-scan.integration.ts` | verified | |
| AUTH-ERROR-003 (policy) | `lib/runtime/configuration.ts` `supportEmailForServer()` (added by the support-contact-policy-value pull request) reads `SUPPORT_EMAIL`, falling back to `support@idoc.club` when unset — deliberately not `required()`-gated, since a user-facing support address is not a security-critical secret that should fail a production build closed. `app/(login)/sign-in/actions.ts`'s persistent-error UX now interpolates it instead of a hardcoded string. Distinct from `app/.well-known/security.txt/route.ts:10`'s operator-facing `IDOC_ADMIN_NOTIFICATION_EMAIL`, a security-researcher contact, not a member-facing support value | N/A | `tests/runtime-configuration.test.ts` (explicit value, fallback, and whitespace-only-treated-as-unset); `tests/unified-migrated-login.test.ts` source-inspection-proves the failure branch interpolates it | verified | A portable, policy-configured `supportEmail` now exists and is used by the persistent-error UX this row named; an operator sets `SUPPORT_EMAIL` to the real address, with a working default in the meantime. |
| AUTH-REDIRECT-001 | `lib/auth/mfa/step-up.ts:43-45` `safeReturnTo`; `lib/auth/google-oidc-reference.ts:214-227` `normalizeReturnTo` | N/A | `tests/security-e2e/google-oauth.spec.ts` | verified | |
| AUTH-REDIRECT-002 | Same as above — origin-match required, protocol-relative (`//`) explicitly rejected | N/A | `tests/security-e2e/google-oauth.spec.ts` | verified | |
| AUTH-TRANSPORT-001 | `next.config.ts:15` HSTS header (`max-age=63072000; includeSubDomains; preload`); `secure: NODE_ENV==='production'` cookie flags throughout | N/A | `tests/security-e2e/cookies-and-headers.spec.ts:3` | verified | |
| AUTH-TRANSPORT-002 | `app/api/user/route.ts` and `app/api/auth/google/link/status/route.ts` (added by the no-store-auth-responses pull request) now set an explicit `Cache-Control: no-store` header on every response, on top of Next.js's implicit dynamic-rendering behavior from `cookies()` usage | N/A | `tests/security-e2e/cookies-and-headers.spec.ts` (a dedicated test asserts `cache-control: no-store` on both routes against the real running app) | verified | Both JSON endpoints that reflect the caller's own session/linked-identity state now carry the explicit directive, behaviorally proven. |
| AUTH-API-001 | Consistent pattern across `app/api/*/route.ts` and Server Actions: trusted server derives identity/role, client only receives shaped output (e.g. `app/api/user/route.ts`) | N/A | `tests/authorization-boundary-inventory.test.ts` | verified | |
| AUTH-FRAMEWORK-003 | `middleware.ts` explicit cookie/CSRF handling; `lib/security/request-origin.ts` explicit, Vercel-gated proxy header handling (AUTH-RATE-004, fixed in a later pull request); `next.config.ts` explicit headers | N/A | `tests/security-e2e/csrf.spec.ts`, `cookies-and-headers.spec.ts`, `tests/trusted-proxy-headers.test.ts` | verified | Cookies/CSRF/caching/proxy-trust are all now explicit. |
| AUTH-STORAGE-003 | Secrets encrypted/hashed at rest: `lib/auth/password-hash.ts` (Argon2id), `lib/security/encrypted-payload.ts` (delivery payloads), TOTP secrets `encryptedSecret`/`keyId` in `lib/auth/mfa/types.ts:21-22` | `idoc.mfa_totp_factors`, `idoc.account_tokens` (hash only) | `tests/canonical-mfa-runtime.test.ts:163` ("TOTP secrets encrypted with authenticated encryption and key IDs"); `tests/evidence-safety-scan.integration.ts` (no plaintext secrets in logs) | verified | |
| AUTH-OPERATIONS-002 | All revocation/rate-limit/transaction-consumption state lives in Postgres (`idoc.auth_sessions`, `idoc.account_request_limits`, `idoc.mfa_*`), not in-process memory, so it's consistent across serverless instances; DB errors propagate (fail closed) per `lib/auth/session.ts:41-42` comment | multiple tables | `tests/session-registry-adversarial.integration.ts` | verified | |

**Summary of most significant gaps:** (1) **AUTH-RATE-004 — fixed in a later pull request** (`requestOrigin()` no longer trusts `X-Forwarded-For`/`X-Real-IP` unless `process.env.VERCEL` is set, and always validates IP shape; see the row above). (2) **AUTH-CSRF-003** — no token-based CSRF mechanism exists; the app relies solely on Origin-header validation (effective, but not what this control literally requires). (3) **AUTH-ERROR-003** — no policy-configured, non-secret `supportEmail` used in persistent-error UX. (4) **AUTH-TRANSPORT-002** — no explicit `Cache-Control: no-store` on sensitive API/pages, only implicit Next.js dynamic-rendering avoidance. (5) **AUTH-RATE-002** — no `Retry-After`-style server contract, only in-band cooldown messages. Everything else audited (session lifecycle, MFA, recovery, OAuth CSRF/state binding, password hashing, bot defense, cookie hardening) is well-implemented with strong behavioral (often real-Postgres/real-browser) test coverage.


## Stage 3 — Signup, Email Verification, OTP, Password

| ID | Implementation location | Persistence location | Test evidence | Assessment | Notes |
|---|---|---|---|---|---|
| AUTH-IDENTITY-002 | `app/(login)/sign-up/actions.ts:84-102` (role always `'member'`); `lib/membership/role-grants.ts:28-61` (`requireSuperAdmin` gate for privileged roles) | `lib/db/schema.ts` `users`, `application_roles` (partial unique index prevents duplicate active grants) | `tests/password-security-hardening.test.ts` (source-inspection); no behavioral test asserting signup can never create an admin row | implemented-but-unverified | Signup never inserts `application_roles`; privileged grants require an authenticated Super Admin + fresh step-up. No test proves the negative (signup cannot self-elevate) behaviorally. |
| AUTH-IDENTITY-003 | `lib/membership/validation.ts:86-88` `normalizeEmail` (trim + ASCII lowercase); no dot/plus stripping | `lib/db/schema.ts:23,33` `users.email` unique + `uniqueIndex('users_normalized_email_unique') on lower(email)` | `tests/rate-limit-normalization.integration.ts` (behavioral, real Postgres) proves case/whitespace variants collapse to one identity | partial | Case-folding is plain `.toLowerCase()`, not full Unicode-aware case folding (e.g., no `.normalize()`/locale casing for non-ASCII local parts); "preserve display form" is not implemented — normalized (lowercased) form is what's stored and shown, original casing is discarded. |
| AUTH-LIFECYCLE-001 | `app/(login)/sign-up/actions.ts:84-102`; `lib/auth/pending-signup.ts` (signed HttpOnly JWT cookie, 15-min TTL, no DB row) | N/A — pending signup deliberately not a DB row (only committed `users` row is `accountState:'onboarding'`, created only after OTP verification + password) | `tests/signup-otp-regression.test.ts`, `tests/account-token-lifecycles.integration.ts` (behavioral) | verified | Stronger than the base requirement: no DB row at all exists until email verification + password both complete, so no pending row can ever leak "normal access." |
| AUTH-LIFECYCLE-002 | `app/(login)/actions.ts:61` (`signIn`, accountState allowlist `['active','onboarding','migrated_pending']`); `app/(login)/sign-up/actions.ts:87-88` (existing-email check) | `lib/db/migrations/0024_account_state_check_constraint.sql` (DB-level CHECK on `account_state`) | `tests/password-security-hardening.test.ts` (source-inspection only for the DB constraint); no integration test exercising login with a `suspended`/`deleted` account | implemented-but-unverified | Logic looks correct and DB-enforced, but no behavioral test drives a `suspended`/`deleted` account through `signIn` to confirm rejection end-to-end. |
| AUTH-EMAIL-001 | `lib/auth/email-otp.ts:61-117` (`issueEmailOtp`/`verifyEmailOtp`) | `lib/db/schema.ts` `email_otp_codes` (purpose/email/userId-bound, `expiresAt`, `consumedAt`, `attemptCount`) | `tests/email-otp-cross-purpose.integration.ts` (behavioral, real Postgres) | verified | Purpose-binding, expiry, single-use, replay-resistance, and account-scoping all directly exercised. |
| AUTH-EMAIL-002 | `lib/auth/email-otp.ts:67-69` (30s cooldown), `:73-74` (supersede prior unconsumed code) | same table | `tests/email-otp-cross-purpose.integration.ts` partially; no dedicated resend-throttle/enumeration-resistance integration test found | partial | Cooldown + supersession code exists and is exercised indirectly, but no test specifically drives `issueEmailOtp` resend/cooldown timing or confirms enumeration-resistant response shape for signup resend. |
| AUTH-EMAIL-003 | `lib/membership/email-verification.ts` (`issueEmailVerification`/`consumeEmailVerification`); `app/(login)/actions.ts:258-263` (`updateAccount`, `requireFreshStepUp('change-email')`) | `lib/db/schema.ts` `email_verification_tokens` | `tests/email-change.integration.ts` (behavioral, real Postgres, incl. race-condition test) | verified | Fresh step-up required, unique-address race resolved atomically, supersession, dual old/new-address notification all exercised. |
| AUTH-OTP-001 | `lib/auth/email-otp.ts:26,72,75-78` (SHA-256 hashed code storage, CSPRNG `randomInt`, 15-min TTL, 5-attempt bound, resend cooldown) | `email_otp_codes.code_hash` | `tests/email-otp-cross-purpose.integration.ts` (behavioral) | verified | Note: SHA-256 of a 6-digit numeric code is not itself brute-force-resistant in isolation, but bounded attempts + expiry mitigate this per design intent. |
| AUTH-OTP-002 | `app/(login)/actions.ts:74-95` (email OTP gates login only when `emailVerifiedAt` null / migrated_pending — separate from `beginPrimaryMfa`/TOTP path) | N/A | No dedicated test asserting email OTP alone cannot satisfy a TOTP/MFA requirement | implemented-but-unverified | Code paths are separate (`issueEmailOtp` for ownership vs `mfaStore`/TOTP for MFA) and never cross-wired in the code read, but no behavioral test proves email-OTP completion cannot be reused to bypass `beginPrimaryMfa`. |
| AUTH-PASSWORD-001 | `lib/auth/password-policy.ts:18-32` (`passwordSchema`) | N/A | `tests/password-composition-policy.test.ts` (behavioral, calls schema directly) | verified | Correctly implements the canonical-contract.ts **override** (12-128 chars + uppercase/lowercase/number/special composition), not the un-overridden base statement — verified against `canonicalPasswordRequirementStatement` in the reference repo. |
| AUTH-PASSWORD-002 | `lib/auth/password-policy.ts:14-16` (`countPasswordCharacters`, code-point counting); `passwordEntrySchema` for verification path | N/A | `tests/password-composition-policy.test.ts` (behavioral, astral-plane surrogate-pair cases) | verified | Confirmed commits 6fa9900/e852275/9aa8b5a fix is real: `Array.from(value).length` used consistently for min/max, tested with U+1D538 surrogate-pair cases on both creation (`passwordSchema`) and verification (`passwordEntrySchema`) paths; client `PasswordField` has no `maxLength` truncation. |
| AUTH-PASSWORD-003 | `lib/security/password-breach-check.ts` (HIBP k-anonymity range query, 5-char SHA-1 prefix only); called at `app/(login)/sign-up/actions.ts:89`, `app/(login)/actions.ts:203`, `app/(login)/recover-password/actions.ts:153`, `lib/membership/account-recovery.ts:158` | N/A | `tests/password-breach-check.test.ts` (behavioral, mocked fetch), `tests/password-security-hardening.test.ts` (source-inspection for call-site ordering) | verified | Fails open (`checked:false`) on provider unavailability by design — matches requirement's "provider failure MUST NOT disclose the password," though this does mean an outage silently skips the check. |
| AUTH-STORAGE-004 | `lib/auth/password-hash.ts:36-92` (Argon2id via Node's native `crypto.argon2`, versioned `argon2id$v=19$m=...` format, 16-byte random salt, `timingSafeEqual`) | `users.password_hash` (text) | `tests/password-hash.test.ts` (behavioral, real Argon2id derivation + bcrypt legacy compat) | verified | Constant-time compare confirmed; legacy bcrypt still verifiable for pre-migration credentials. |
| AUTH-STORAGE-005 | `lib/auth/password-hash.ts:90-92` (`passwordHashNeedsUpgrade`); `app/(login)/actions.ts:65-70` (rehash-on-successful-login, conditioned on unchanged hash via `WHERE ... eq(passwordHash, foundUser.passwordHash)`) | `users.password_hash` self-describing format string | `tests/password-hash.test.ts` (behavioral, unit) proves detection; no integration test exercises the actual DB rehash-on-login path | implemented-but-unverified | No pepper used (acceptable — optional per requirement). Rehash logic itself untested against real Postgres/`signIn`. |
| AUTH-INPUT-001 | `lib/membership/validation.ts:86-88` (email normalize); `components/auth/password-field.tsx:6-53` (`autoComplete="new-password"/"current-password"`, no truncation) | N/A | `tests/evidence-safety-scan.integration.ts` (behavioral, real Postgres, sweeps logs/state for leaked passwords) | verified | Password/email exclusion from persisted evidence directly proven; client field allows paste (no `onPaste` blocking, no maxLength). |
| AUTH-CRYPTO-002 | `lib/observability/logger.ts` (convention: only static event + coarse metadata, never raw bodies/exceptions) | N/A | `tests/evidence-safety-scan.integration.ts` (behavioral, real Postgres, asserts no password/token/secret/raw-exception substring across audit/notification/rate-limit/delivery tables) | verified | Logger itself is a documented discipline, not a structural redaction guarantee — but the integration test empirically proves no secret leakage across a representative flow sweep. |
| AUTH-TRANSACTION-008 | `lib/auth/email-otp.ts:98-116` (`db.transaction`, attempt-increment then consume, all conditions checked inside tx); `lib/membership/email-verification.ts:50-90`; `lib/membership/account-recovery.ts:169-191` | Respective tables (`email_otp_codes`, `email_verification_tokens`, `account_tokens`) | `tests/email-otp-cross-purpose.integration.ts`, `tests/email-change.integration.ts` (both behavioral, incl. concurrent-race tests) | verified | Race-condition (`Promise.all` concurrent consume) tests directly prove exactly-one-winner atomicity. |
| AUTH-RATE-005 | `lib/security/rate-limit.ts` (`checkRateLimit`, dual email+origin hashed buckets); called at every signup/login/resend/recovery call site | `idoc.account_request_limits` (hashed identifier/origin, not raw) | `tests/rate-limit-normalization.integration.ts` (behavioral, real Postgres, incl. dedicated `login_password` AUTH-RATE-005 test) | verified | No permanent lockout observed — bounded time-window buckets only; escalation to "suspicious-activity" beyond rate-limiting/breach-alert email not found (no account lockout, no anomaly-scoring layer). |
| AUTH-SESSION-008 | `app/(login)/actions.ts:59-136` (`signIn`, `comparePasswords` → `beginPrimaryMfa` decision → `setSession` only if MFA not required); `lib/auth/mfa/login.ts:25-49`, `lib/auth/mfa/decision.ts` | `lib/auth/mfa/pending-primary-auth.ts` transaction state; `users.session_version` | `tests/canonical-mfa-runtime.test.ts`, `tests/fresh-mfa-step-up.test.ts` — need to confirm these are behavioral (not verified in this pass) | implemented-but-unverified | Code path clearly never calls `setSession` before an MFA decision is resolved (`decideMfa` gates challenge/enrollment before full session); did not have time to confirm depth of behavioral test coverage on `decideMfa` branch logic specifically. |
| AUTH-PASSWORD-004 | `app/(login)/actions.ts:39-63` (`signIn`: `normalizeEmail`, dual-bucket rate limit before compare, generic `"Invalid email or password"` error, `comparePasswords` in trusted server code, accountState eligibility check) | `email_otp_codes`/`account_request_limits` for attempt evidence | `tests/rate-limit-normalization.integration.ts` (behavioral, dedicated `login_password` test) | verified | Enumeration-resistant generic error confirmed by code read; fail-closed on unknown hash format confirmed by `password-hash.test.ts`. |
| AUTH-RECOVERY-002 | `lib/membership/account-recovery.ts:31-68` (`requestAccountLink`) | `idoc.account_tokens`, `idoc.account_delivery_outbox` (encrypted payload) | `tests/account-token-lifecycles.integration.ts` ("password recovery is neutral, purpose-separated, rate-limited, digest-only") — behavioral | verified | Timing-equalized (`equalizeAnonymousResponse`) neutral response for ineligible requests; encrypted delivery payload confirmed. |
| AUTH-RECOVERY-003 | `lib/membership/account-recovery.ts:152-192` (`consumeAccountToken`) | `idoc.account_tokens` | `tests/account-token-lifecycles.integration.ts` ("password reset has one concurrent winner, rotates credentials, invalidates sessions, and rejects replay and cross-purpose tokens") — behavioral | verified | `sessionVersion` increment revokes sessions; `password_reset_completed` notification queued; MFA rows untouched (confirmed by absence of any MFA-table mutation in the function). |
| AUTH-PASSWORD-005 | `app/(login)/actions.ts:192-226` (`updatePassword`) | `users.password_hash`, `auth_security_notification_outbox` | `tests/password-security-hardening.test.ts` (source-inspection only) — no integration test found exercising `updatePassword` against real Postgres | implemented-but-unverified | Subject from authenticated session (`validatedActionWithUser`), fresh step-up required, current-password re-verification, `sessionVersion` rotation + `clearSession`, notification outbox — all present in code, but not behaviorally tested. |
| AUTH-EMAIL-004 | `lib/notifications/auth-security-delivery.ts:49-62` (notification content map) | `idoc.auth_security_notification_outbox` | `tests/evidence-safety-scan.integration.ts` (behavioral) confirms no password/secret leaks in notification_outbox; no test asserts absence of "untrusted actionable URL" specifically | verified | Content templates contain no password, no reusable secret, no action URL — only static heading + "contact IDOC" guidance, satisfying the requirement by construction. |
| AUTH-API-002 | `lib/auth/middleware.ts:37-55` (`validatedActionWithUser` derives `user` from server-side `getUser()`, never from client `formData`) | N/A | No dedicated behavioral test found asserting a forged client-supplied subject/user field is ignored | implemented-but-unverified | IDOC uses Next.js Server Actions (not a JSON REST surface) for these flows, so classic "API shape" concerns are structurally mitigated by the framework, but no explicit adversarial test targets this. |
| AUTH-OPERATIONS-003 | `lib/security/password-breach-check.ts:35-54` (fails open, never discloses password); `lib/membership/account-recovery.ts:59-67` (`logError` on failure, no identifier/token/exception in log); `lib/membership/email-verification.ts:40-43` (delivery failure leaves token valid, doesn't falsely confirm) | N/A | `tests/evidence-safety-scan.integration.ts`, `tests/account-token-lifecycles.integration.ts` (behavioral, provider-failure branches) | verified | Provider/DB failure paths explicitly tested to never grant authority, never falsely confirm delivery, and never leak credentials into evidence. |

### Stage 3 summary of significant gaps

The strongest evidence is around OTP/verification transaction atomicity, breach-check integration, Argon2id storage, and evidence-safety (all proven with real-Postgres behavioral tests). The most notable gaps: **AUTH-IDENTITY-003** only does ASCII lowercasing (not full Unicode-aware case-folding) and discards original display casing; **AUTH-RATE-005** has no suspicious-activity escalation beyond flat time-window buckets; **AUTH-PASSWORD-005** (password change) and **AUTH-STORAGE-005** (rehash-on-login) are implemented in code but have zero integration-level behavioral tests against real Postgres, relying only on source-inspection tests (`password-security-hardening.test.ts`) that check regex patterns rather than executing the logic; and **AUTH-API-002**/**AUTH-SESSION-008**/**AUTH-OTP-002** lack dedicated adversarial tests proving negative properties (client can't select subject; email OTP can't substitute for MFA) even though code inspection shows no obvious cross-wiring.


## Stage 4 — MFA, TOTP, Recovery, Remembered-Device

Also covers Slice 3's corrupted-state/failure-mode scenario analysis (final table below).

# Stage 4 Audit — MFA / TOTP / Recovery / Remembered-Device

**Scope note on evidence quality**: Almost every source-referenced test that mentions the production finalization functions (`finalizeAuthenticatorReplacement`, `finalizeInitialAuthenticatorEnrollment`, `consumeRecoveryCodeWithEvidence`, `requireFreshStepUp`/`consumeStepUpAuthority`) is **source-inspection-only** (`readFileSync` + `assert.match(source, /regex/)` against the file's text). The real-Postgres integration tests (`tests/mfa-store.integration.ts`, `tests/webauthn-store.integration.ts`) exercise a **parallel, largely unused-in-production** code path: `PostgresMfaStore.consumeEnrollmentAndActivate` / `completeTotpEnrollment` (`lib/auth/mfa/totp.ts`), which the real login flow (`app/(login)/mfa/actions.ts`) does **not** call — it calls `finalizeAuthenticatorReplacement`/`finalizeInitialAuthenticatorEnrollment` directly. I flag this explicitly per requirement below.

**Former major structural finding, now resolved**: The TOTP "remembered device" subsystem (`lib/auth/mfa/remembered-device.ts`, `mfaStore.createRememberedDevice/consumeRememberedDevice`, the inline revocation in `revokeFactor`, table `idoc.mfa_remembered_devices`) had long been fully implemented and unit/integration-tested but was dead code in production — the only caller of `decideMfa` (`beginPrimaryMfa`) hardcoded `rememberTotpDevice: false, rememberedDeviceValid: false`, and `mfaConfiguration()` had no `rememberTotpDevice`/`rememberTotpDays` policy fields at all. The remember-totp-device-wiring pull request closed this: `mfaConfiguration().rememberedDevice` is a real, opt-in (off by default) policy surface, `beginPrimaryMfa` now reads and verifies a real cookie via `lib/auth/mfa/remembered-device-cookie.ts`, and `app/(login)/mfa/actions.ts`'s `verifyLoginTotp` issues that cookie on request. See AUTH-REMEMBER-001/002/003/004 above, all now `verified`. The unrelated `login_trusted_devices` / `login-device-trust.ts` feature ("remember this browser" for ordinary members) remains explicitly commented as "deliberately independent from TOTP factors" (`lib/db/schema.ts:578`) and only ever suppresses the email-OTP/re-auth step for `role === 'member'` accounts, who never require TOTP in the first place — it does not suppress MFA, and is a genuinely separate feature from the one this section describes.

### Requirement matrix

| ID | Implementation location | Persistence location | Test evidence | Assessment | Notes |
|---|---|---|---|---|---|
| AUTH-MFA-003 | `lib/auth/mfa/store.ts` (all factor rows built server-side: `factorRecord()` L34-47; `revokeFactor` L304-316; `consumeEnrollmentAndActivate` L115-148 sets status/lineage under lock) | `idoc.mfa_factors` (`lib/db/schema.ts:446-475`) | `tests/mfa-store.integration.ts` (real Postgres, exercises the unused-in-prod store path) | verified | Factor status/timestamps/lineage are DB-computed, never client-supplied, in both the tested store path and the actually-used finalization files. |
| AUTH-MFA-004 | `lib/auth/mfa/decision.ts:9-22` (`decideMfa`) + `lib/auth/mfa/step-up.ts` (`sensitiveActionRequiresFreshStepUp`, separate function for step-up policy) | N/A (policy logic, no dedicated table) | `tests/canonical-mfa-runtime.test.ts` (unit, real function calls, no DB); `tests/live-privileged-mfa.test.ts` (unit) | partial | Role/enrollment-eligibility/remembered-state/action evaluation exists but is split across two separate functions rather than one canonical evaluator, and the "remembered state" input is permanently hardcoded `false` by the only production caller (`login.ts:29-30`) — so that branch of the policy is implemented but unreachable in production. |
| AUTH-MFA-005 | `lib/auth/mfa/totp.ts:140-190` (`beginTotpEnrollment`, CSPRNG via `generateTotpSecret`→`randomBytes(20)`); atomic activation in `lib/auth/mfa/enrollment-finalization.ts:10-75` and `lib/auth/mfa/replacement-finalization.ts:10-105` (production path) | `idoc.mfa_factors`, `idoc.mfa_enrollment_transactions` (`lib/db/schema.ts:446-491`) | `tests/security-completeness-item-8.test.ts`, `tests/live-privileged-mfa.test.ts` (source-inspection only for the production finalization files); `tests/mfa-store.integration.ts` (real-Postgres, but tests the parallel unused `consumeEnrollmentAndActivate` path) | implemented-but-unverified | Pending factor is expiring (`TOTP_ENROLLMENT_TTL_MS = 10min`), purpose-bound, and displaying the QR/provisioning URI (`beginTotpEnrollment`) never activates it — only `finalizeInitialAuthenticatorEnrollment`/`finalizeAuthenticatorReplacement` do, atomically. No behavioral (real-Postgres) test exists for the actual production functions. |
| AUTH-MFA-006 | `lib/auth/mfa/totp.ts:97-114,234-271` (`verifyTotpCode` ±1 window, `verifyActiveTotp`); attempts/replay in `app/(login)/mfa/actions.ts:100-115,38-51`; rate limit `lib/security/rate-limit.ts` | `idoc.mfa_challenge_transactions` (attempt_count/max_attempts), `idoc.mfa_factors.last_accepted_counter` | `tests/canonical-mfa-runtime.test.ts` (unit, attempt-exhaustion charging); `tests/mfa-store.integration.ts` (real-Postgres concurrency race on `acceptTotpChallenge`, same code path used by login/step-up verification); `tests/security-e2e/webauthn-passkeys.spec.ts` (real browser, real TOTP step-up round-trip) | verified | `equalCode` uses `timingSafeEqual`; challenge replay/attempt exhaustion is enforced under a Postgres row lock and is behaviorally proven for `acceptTotpChallenge`/`recordChallengeFailure`, which **is** the function the login/step-up TOTP flow actually calls (`totp.ts` `verifyActiveTotp` → `store.acceptTotpChallenge`). |
| AUTH-STORAGE-006 | `lib/auth/mfa/totp.ts:116-138` (`encryptTotpSecret`/`decryptTotpSecret`, AES-256-GCM, per-key-ID) | `idoc.mfa_factors.encrypted_secret`/`encryption_key_id`; DB CHECK `mfa_factors_totp_secret_check` (`lib/db/schema.ts:467-469`) forces non-null secret+key for `totp` factors | `tests/canonical-mfa-runtime.test.ts` (unit round-trip); no test scans logs/browser storage for TOTP secrets specifically | implemented-but-unverified | Encryption is real (32-byte key required, GCM auth tag). No behavioral test of "never in logs/telemetry" for TOTP secrets specifically (only `console.*` absence confirmed by code review — no `console.*` calls anywhere in `lib/auth/mfa/`). |
| AUTH-RECOVERY-004 | `lib/auth/mfa/recovery.ts:16-43` (`generateRecoveryCode` → `randomBytes(16)`, 128-bit; `digestRecoveryCode` HMAC-SHA256, one-way); consumption `lib/auth/mfa/recovery-security.ts:9-35` (production) | `idoc.mfa_recovery_codes` (`digest` column only, no plaintext; unique per user+app+digest) | `tests/mfa-store.integration.ts:146-171` (real-Postgres concurrency race, but on `store.consumeRecoveryCode`, not the production `consumeRecoveryCodeWithEvidence`); `tests/canonical-mfa-runtime.test.ts` (unit, entropy/normalization) | implemented-but-unverified | Same architectural split as AUTH-MFA-005: `consumeRecoveryCodeWithEvidence` (production, called from `app/(login)/mfa/actions.ts:157`) uses the identical `for update skip locked` atomic pattern but has no dedicated concurrency test of its own — only `security-completeness-item-8.test.ts` (source-inspection) references it. Rate-limited via `mfa_recovery_code_verify` purpose. Excluded from routine MFA (recovery is a separate `pendingAccount('recovery-entry')` stage, never satisfies `decideMfa`'s `challenge-required`). |
| AUTH-RECOVERY-005 | `app/(login)/mfa/actions.ts:145-171,198-226` (`beginAuthenticatorRecovery`→`authorizeAuthenticatorRecovery`→`confirmTotpEnrollment` stage `'replacement'`→`'recovery-ack'`); `lib/auth/mfa/pending-primary-auth.ts` (signed, 10-min TTL, stage-typed cookie) | Transient — JWT cookie `idoc_pending_primary_mfa`, no DB row for the authority itself | `tests/security-completeness-item-8.test.ts` (source-inspection: `assert.doesNotMatch(...,/setSession|registerSession/)` between `authorizeAuthenticatorRecovery` and `confirmTotpEnrollment`); no behavioral test drives the full recovery→replacement→ack→session sequence end-to-end | implemented-but-unverified | Code path confirmed by direct reading: a consumed recovery code only advances `stage` to `'replacement'`, requiring a fresh TOTP-factor proof + an explicit `acknowledgeRecoveryCodes` (`'recovery-ack'`→`setSession`) step before any session is created — `setSession` never appears between recovery consumption and enrollment completion. No e2e/integration test exercises this; it's verified only by manual code reading + a regex assertion. |
| AUTH-RECOVERY-006 | `lib/auth/mfa/enrollment-finalization.ts:59-65`, `lib/auth/mfa/replacement-finalization.ts:76-83` (`delete` then re-`insert` recovery codes in the same transaction as factor activation) | `idoc.mfa_recovery_codes` | `tests/security-completeness-item-8.test.ts` (source-inspection) | partial | Atomic invalidate+replace genuinely happens, but **only as a side effect of TOTP enrollment/replacement** — there is no standalone "regenerate recovery codes" action. `SensitiveAction: 'generate-recovery-codes'` is declared in `types.ts:11` but has zero call sites anywhere (`grep` confirms). The security UI explicitly states "New codes are issued when you replace your authenticator" (`security-client.tsx:54`), and replacing an authenticator itself requires first burning a recovery code (`beginAuthenticatorReplacement`, `dashboard/security/actions.ts:88-98`) — so a member who has *not* lost their device cannot get a fresh code set without an unnecessary detour through the loss-recovery flow. |
| AUTH-REMEMBER-001 | `lib/runtime/configuration.ts` `rememberedTotpDeviceConfiguration()` (added by the remember-totp-device-wiring pull request), returned as `mfaConfiguration().rememberedDevice`: `enabled` (`REMEMBER_TOTP_DEVICE_ENABLED`, off unless explicitly `'true'`), `days` (`REMEMBER_TOTP_DEVICE_DAYS`, 1-90, default 30), `digestSecret` (required only once enabled, matching every other MFA secret's fail-closed pattern) | N/A | `tests/runtime-configuration.test.ts` (off-by-default, and fail-closed on the day bound/digest key once enabled) | verified | The policy premise now exists as a real, server-owned, opt-in configuration surface, not a hardcoded false. |
| AUTH-REMEMBER-002 | `lib/auth/mfa/remembered-device.ts:8-63` (opaque 32-byte token, HMAC digest-only storage, revocable record) is now actually reachable: `app/(login)/mfa/actions.ts`'s `verifyLoginTotp` calls `issueRememberedDevice` and `lib/auth/mfa/remembered-device-cookie.ts` sets the `__Host-idoc-mfa-remember` cookie (httpOnly, secure, sameSite=lax, path=/, maxAge bound to the configured day count) only when the caller opted in via the checkbox and the policy is enabled | `idoc.mfa_remembered_devices` (`lib/db/schema.ts:523-536`) | `tests/remembered-totp-device.integration.ts` (real-Postgres: issuance, correct-token acceptance, wrong-token/wrong-secret/expired/missing-token rejection, multi-secret rotation); `tests/live-privileged-mfa.test.ts` (source-inspection proving the action only issues after TOTP acceptance and only when the checkbox/policy conditions hold) | verified | The feature is now genuinely offered, transported with hardened cookie attributes, and behaviorally proven. |
| AUTH-REMEMBER-003 | `lib/auth/mfa/decision.ts` `decideMfa` already returned `'remembered-device-satisfied'`; `lib/auth/mfa/login.ts`'s `beginPrimaryMfa` now actually reads the cookie, calls `verifyRememberedDevice`, and treats that outcome the same as `'not-required'` (skips the TOTP challenge for the remainder of the remembered window) | N/A | `tests/live-privileged-mfa.test.ts` (source-inspection proving `beginPrimaryMfa` no longer hardcodes `rememberedDeviceValid`/`rememberTotpDevice` to `false`, and that both `'not-required'` and `'remembered-device-satisfied'` short-circuit the same way) | verified | The suppression path this row describes is now actually reachable and correctly bounded to the configured policy. |
| AUTH-REMEMBER-004 | `store.ts`'s `revokeFactor` directly revokes any `mfa_remembered_devices` row bound to the factor it revokes (`store.ts:312-313`), and `consumeRememberedDevice`'s own query requires the bound factor's `status='active'` (`store.ts:268-271`), so a factor replacement (which flips the old factor to `status='replaced'`) implicitly invalidates any remembered device tied to it even without a separate explicit revoke call — both mechanisms are real, not merely present-in-isolation | `idoc.mfa_remembered_devices.revoked_at`/`revoke_reason` | `tests/mfa-store.integration.ts:173-224` (store-level); `tests/remembered-totp-device.integration.ts` (proves the same invariant one layer up, through `issueRememberedDevice`/`verifyRememberedDevice` rather than raw store calls) | verified | Revocation-on-replacement is now exercised by a live, reachable feature, not moot dead code. |
| AUTH-LIFECYCLE-003 | `lib/auth/mfa/replacement-finalization.ts:10-105` (production): preserves old factor active until same-transaction activation of new factor; rotates recovery codes, revokes remembered devices tied to old factor, bumps `session_version`, revokes all `auth_sessions`, writes audit + notification | `idoc.mfa_factors`, `idoc.mfa_recovery_codes`, `idoc.mfa_remembered_devices`, `idoc.auth_sessions`, `idoc.users.session_version`, `idoc.audit_log`, `idoc.auth_security_notification_outbox` | `tests/security-completeness-item-8.test.ts`, `tests/live-privileged-mfa.test.ts` (source-inspection only); **no** integration/e2e test drives `finalizeAuthenticatorReplacement` itself — `tests/security-e2e/global-setup.ts:58-63` seeds an already-active TOTP factor directly via SQL, bypassing enrollment/replacement entirely, so no e2e test ever executes this function | implemented-but-unverified | Code review confirms the transaction is correct (old factor locked and re-checked via `for update`, optimistic `session_version` concurrency check at L85-89 handles a concurrent old-factor change by failing the whole transaction). No behavioral proof. |
| AUTH-LIFECYCLE-004 | Not supported — `revokeFactor` exists in `store.ts:304-316` but is called from **no production code**, only `tests/mfa-store.integration.ts:195-203` | `idoc.mfa_factors.status='revoked'` possible via `revokeFactor`, but never reached | N/A | not-applicable | IDOC's model makes TOTP mandatory for admin/super-admin with no self-service or admin-initiated "disable MFA" action; `lib/membership/role-grants.ts` (privilege demotion) does not touch MFA factors either. Given MFA is compulsory-by-role rather than optional-per-user, disablement is out of scope for this product model — condition "(if supported)" in the requirement is not met. |
| AUTH-STEPUP-002 | `lib/auth/mfa/step-up.ts:82-125,141-158` (`requireFreshStepUp`/`grantFreshStepUp`/`consumeFreshStepUp`); explicitly excludes remembered-device (`fresh-mfa-step-up.test.ts` asserts `stepUp` source never mentions "remembered") | `idoc.mfa_challenge_transactions` (`purpose='step-up'`) for the persisted challenge; authority itself is a signed cookie, 5-min TTL | `tests/fresh-mfa-step-up.test.ts` (source-inspection); `tests/security-e2e/webauthn-passkeys.spec.ts` (real browser: proves step-up is required per distinct sensitive action — add-passkey and remove-passkey each force a fresh TOTP round; proves `consumeStepUpAuthority`'s single-claim behavior works against real Postgres) | verified | The e2e test is genuinely behavioral for freshness expiring-per-action-use and server validation; action-binding (`evidence.action === action` at `step-up.ts:76`) is only exercised with one action (`change-mfa`) in that test, so cross-action non-replayability is code-reviewed/source-asserted but not behaviorally proven with two *different* actions. |
| AUTH-API-003 | `app/(dashboard)/dashboard/security/page.tsx:16-27` (only `totpConfigured: Boolean(factor)` boolean + safe passkey subset `{createdAtMs, credentialId, deviceName, lastUsedAtMs}` sent to client); no `app/api/*` route touches `mfaFactors`/`encryptedSecret`/`factorId` (`grep` confirms zero matches) | N/A | `tests/account-security-management.test.ts:31-36` (source-inspection, `FORBIDDEN_RENDERED_SECRETS` regex against client component) | implemented-but-unverified | Code review is clean; no behavioral test actually renders the page and inspects the real HTTP response for leaked fields. |
| AUTH-CRYPTO-003 | No `console.*` calls anywhere under `lib/auth/mfa/` (`grep` confirms); secrets/tokens/codes are only ever passed as function parameters, never serialized to a log sink | N/A | `tests/auth-security-events.integration.ts` (real-Postgres, but only exercises `password_changed`/`verified_email_changed` kinds — MFA-specific kinds `authenticator_enrolled`/`authenticator_replaced`/`recovery_code_used`/`passkey_registered`/`passkey_removed` are never behaviorally checked for secret-exclusion, only asserted via source regex in `security-completeness-item-8.test.ts`) | implemented-but-unverified | No logging statements to leak from, and no telemetry/analytics wiring visible in the MFA lib, but the specific "MFA event outbox rows are secret-free" claim is unverified behaviorally for MFA kinds. |
| AUTH-TRANSACTION-009 | `lib/auth/mfa/enrollment-finalization.ts`, `replacement-finalization.ts`, `recovery-security.ts` — all wrap `client.begin(async (tx) => ...)` with `for update` locks and re-validated bindings | `idoc.mfa_enrollment_transactions`, `idoc.mfa_challenge_transactions`, `idoc.mfa_factors`, `idoc.mfa_recovery_codes` | `tests/mfa-store.integration.ts` (real-Postgres concurrency races, but on the parallel unused store functions, not these production files); production files only have source-inspection coverage | implemented-but-unverified | The concurrency *pattern* (row lock + re-check under lock) is identical between the tested store path and the untested production path, giving reasonable confidence, but this is inference, not proof — a real concurrency bug in the production-only files (e.g. lock-ordering deadlock, an unlocked read) would not be caught by any current test. |
| AUTH-RATE-006 | `app/(login)/mfa/actions.ts` (`allowed()` helper, purposes `mfa_login_verify`/`mfa_step_up_verify`/`mfa_recovery_code_verify`/`mfa_enrollment_confirm`) layered on top of `idoc.mfa_challenge_transactions.attempt_count`/`max_attempts` | `idoc.account_request_limits` (dual email+origin buckets, `lib/security/rate-limit.ts:17-45`) | `tests/rate-limit-normalization.integration.ts` (real-Postgres, generic `checkRateLimit` mechanism proven dual-bucket/atomic/secret-free) — but exercises the mechanism with arbitrary purpose strings, not the actual `mfa_*` purposes | implemented-but-unverified | The underlying rate-limit primitive is well-proven behaviorally; the MFA-specific purpose wiring itself is only visible via source reading of `mfa/actions.ts`, not exercised end-to-end. Identifier is the numeric `userId`, not attacker-controllable factor metadata, so no inventory-leak vector is evident. |
| AUTH-OPERATIONS-004 | `lib/notifications/auth-security-events.ts` (`enqueueAuthSecurityNotification`, kinds include `authenticator_enrolled`, `authenticator_replaced`, `recovery_code_used`, `passkey_registered`, `passkey_removed`); calls embedded transactionally in `enrollment-finalization.ts:69-71`, `replacement-finalization.ts:98-101`, `recovery-security.ts:30-32`, `dashboard/security/actions.ts:141-142,157-158` | `idoc.auth_security_notification_outbox`, `idoc.audit_log` | `tests/auth-security-events.integration.ts` (real-Postgres, generic mechanism: dedupe, secret-free, recipient-owned — but exercised only with non-MFA kinds); MFA-specific call sites are source-inspection only (`security-completeness-item-8.test.ts`) | implemented-but-unverified | Mechanism is behaviorally sound and secret-free by design (dedicated `AuthSecurityKind` enum, no arbitrary payload). No distinct "replay detected" notification kind exists (only generic MFA success/failure kinds) — replay attempts surface only as ordinary failed-verification responses, not a dedicated security event. |

### Failure-mode / corrupted-state analysis (Slice 3 scope)

| # | Scenario | Fails closed? | Behavioral test |
|---|---|---|---|
| 1 | Corrupted encrypted TOTP secret (malformed `keyId.iv.tag.ciphertext`) | Yes, by omission — `decryptTotpSecret` (`totp.ts:125-138`) throws `Invalid encrypted TOTP secret.`; no call site in `mfa/actions.ts` catches it, so the server action throws unhandled → no session/authority granted, but surfaces as an unhandled exception/500 rather than a clean user message | NONE |
| 2 | Unknown encryption key ID (factor references a `keyId` absent from `MFA_TOTP_ENCRYPTION_KEYS`) | Yes, by omission — the `resolveKey` closures in `mfa/actions.ts:44,106,180` throw `MFA key unavailable.`, uncaught, propagating up | NONE |
| 3 | Malformed factor metadata (e.g. impossible status combination) | Partially — DB CHECK constraints (`mfa_factors_status_check`, `mfa_factors_totp_secret_check`, `lib/db/schema.ts:463,467-469`) prevent most invalid states from being persisted at all; no app-level defensive handling beyond that | NONE |
| 4 | Missing active encryption key (env var absent/misconfigured at boot) | Yes — `mfaConfiguration()` (`configuration.ts:82-114`) throws at call time if `activeKeyId` isn't in the key map | `tests/canonical-mfa-runtime.test.ts` / `security-completeness-item-8.test.ts:81-85` (unit, real function call — this one genuinely is behavioral, not source-inspection) |
| 5 | Corrupted recovery-code digest state (e.g. digest column truncated/mismatched) | Yes, by omission — `consumeRecoveryCodeWithEvidence`'s `WHERE ... digest in (...)` simply won't match, returns `'invalid'`; no crash, no bypass | NONE |
| 6 | Factor record referencing an unavailable key (same as #2, rotation scenario) | Yes, by omission (see #2) | NONE |
| 7 | Database failure while loading authoritative factor state (`getActiveTotp`/`beginPrimaryMfa` DB error) | Yes, by omission — no try/catch anywhere in `mfa/login.ts`, `mfa/store.ts`, `totp.ts`; an exception propagates and no session is granted | NONE |
| 8 | Replayed TOTP counter | Yes — `mfa_factors.last_accepted_counter` monotonic check under `for update` lock (`store.ts:174`) | `tests/mfa-store.integration.ts:125-128` (real Postgres, but on `acceptTotpChallenge`, which **is** the function the production login/step-up path calls via `totp.ts:verifyActiveTotp`, so this genuinely covers the live path) |
| 9 | Replayed recovery code | Yes — `for update skip locked` + `consumed_at is null` filter, both in the tested `store.consumeRecoveryCode` and the production `consumeRecoveryCodeWithEvidence` (identical pattern) | `tests/mfa-store.integration.ts:146-171` (real Postgres, but on `store.consumeRecoveryCode`, **not** the production `consumeRecoveryCodeWithEvidence` — same SQL shape, different function, unverified directly) |
| 10 | Concurrent recovery-code use (two simultaneous submissions of the same code) | Yes (same mechanism as #9) | Same caveat as #9 — proven only for the non-production twin function |
| 11 | Replacement attempt when the old factor changes concurrently | Yes — `finalizeAuthenticatorReplacement` re-locks the active factor row and gates the whole transaction on `session_version` matching the caller's `expectedSessionVersion` (`replacement-finalization.ts:34,85-89`), so a concurrent change (another replacement, a revoke, a session-version bump) aborts this one | NONE (the store.ts analog, `consumeEnrollmentAndActivate`, has `tests/mfa-store.integration.ts:226-241` "concurrent replacement confirmations leave exactly one active factor" — but that is the non-production function) |
| 12 | Remembered-device evidence with a stale factor version | N/A in practice — feature is unreachable in production (see AUTH-REMEMBER-002/003 above); the store-level code does re-join against `mfa_factors...status='active'` (`store.ts:268-271`), which would catch a stale/replaced factor if the feature were live | `tests/mfa-store.integration.ts:205-224` ("replacement enrollment atomically transitions the old active factor", real Postgres — but exercises dead code) |
| 13 | Passkey sign-count regression/replay | Yes — `updateSignCount` (`webauthn-store.ts:146-159`) rejects a non-increasing counter (with the spec's zero-counter allowance) and does not mutate state on rejection | `tests/webauthn-store.integration.ts:81-104` (real Postgres, genuinely behavioral, and this **is** the function used by the real WebAuthn login/step-up path — strong evidence) |
| 14 | Factor recovery that attempts to create a normal session prematurely | Yes — confirmed by direct code reading: `authorizeAuthenticatorRecovery` and the `'replacement'`-stage branch of `confirmTotpEnrollment` never call `setSession`; only `acknowledgeRecoveryCodes` does, after `acknowledgeRecoveryCodes` re-checks `activeFactor.factorId === context.pending.factorId` (`mfa/actions.ts:228-237`) | NONE (only `security-completeness-item-8.test.ts` source-inspection regex `assert.doesNotMatch(...,/setSession/)` between the two functions — not an execution proving it) |


## Stage 5 — OAuth, Invitation, Tenant, Authorization

### IDOC tenancy/invitation model (Stage 5 detail)

IDOC is a **single-tenant, single-application** platform (one Vercel deployment, one Postgres schema `idoc`, docs/07 §15 "one Vercel-hosted Next.js deployment"). There is no organization/tenant entity anywhere in `lib/db/schema.ts` used as an authorization boundary; `application_roles` is a flat per-user role grant (`member` | `administrator` | `super_admin`, enforced by DB CHECK `application_roles_role_check`) with no scoping column. There is **no invitation flow of any kind**: the `invitations` table and `'invite-privileged-user'`/`ActivityType.INVITE_TEAM_MEMBER` enum values are genuinely unreferenced dead code (`grep` confirms zero call sites outside `lib/db/schema.ts` and the type definitions themselves). Administrator/Super Admin accounts are instead provisioned by an existing Super Admin calling `grantApplicationRole` (`lib/membership/role-grants.ts`) directly against a known user — this is documented, deliberate policy (docs/05 §4, docs/07 §11) requiring Super-Admin authorization + fresh TOTP step-up (`change-privileged-permissions`), not an omitted invitation feature. This makes AUTH-INVITE-001/002/003 **not-applicable**.

**Correction (this revision, per Codex review P1 finding on this PR):** the `teams`/`team_members` tables are **not** fully dead code as an earlier draft of this row claimed. `app/layout.tsx:31-32` calls `getTeamForUser()` (`lib/db/queries.ts:90-117`) inside the root layout's `SWRConfig` `fallback` prop on every request — a live server-side query against `idoc.team_members`/`idoc.teams`, scoped to `teamMembers.userId = <requesting user>`, whose result is serialized into the initial page payload for hydration regardless of whether any component actually subscribes to `/api/team` (the route itself does unconditionally return 404, but the layout-level SWR fallback is a separate, real code path that bypasses it). No production code path ever inserts a row into `team_members` — only `lib/db/seed.ts`, a local/dev seed script, does — so for any real signed-up user this query currently returns `null` and nothing is exposed today. But this is a live, unreviewed data path, not an inert one: if `team_members` were ever populated (a seed script run against production by mistake, or this dead SaaS-starter feature repurposed later without review), the same-team members' `id`/`name`/`email` would be serialized to the client for any authenticated request, with no authorization check beyond "some row exists for this user ID." AUTH-TENANT-001/002 is corrected from `not-applicable` to `partial` on this basis — see the row below — and removing `getTeamForUser()`/the `/api/team` SWR fallback (or gating it behind the same authorization boundary as the rest of the app) is recommended as follow-up cleanup rather than left as an unassessed live path. AUTH-AUTHZ-004's "single-app roles: member/admin/super-admin" model remains exactly what IDOC implements for its real authorization boundary (the `organization-leader` role literal is separately vestigial dead code in `lib/auth/mfa/types.ts`/`decision.ts`, never a grantable role) — this correction is about the inert `teams` read path, not about a real multi-tenant authorization gap. **Update: fixed in a later pull request** — the `getTeamForUser()` call site and its now-fully-dead callee functions were removed; see the AUTH-TENANT-001/002 row below, now `not-applicable` again.

| ID | Implementation location | Persistence location | Test evidence | Assessment | Notes |
|---|---|---|---|---|---|
| AUTH-OAUTH-001 | `lib/auth/google-oidc-reference.ts:68-79` (`GOOGLE_OIDC_PROVIDER`, fixed issuer/endpoints/scopes/PKCE/algs); `lib/auth/google-oidc-reference.ts:149-155` (`loadGoogleOidcConfig`, server env only) | N/A (config, not persisted) | `tests/google-oidc-adoption.test.ts` (source-inspection); `tests/security-e2e/google-oauth.spec.ts` (behavioral e2e, real browser) | verified | Client secret never in `NEXT_PUBLIC_*`/browser (grep confirmed); `resolveGoogleOidcProvider` gates any test-provider override to loopback-only + `!VERCEL`. |
| AUTH-TRANSACTION-010 | `lib/auth/google-oidc-reference.ts:234-296` (`createGoogleAuthorizationRequest`: CSPRNG `state`/`nonce`, S256 PKCE, exact app/origin/redirect/purpose binding) | `idoc.google_oauth_transactions` (`lib/db/migrations/0017_google_oidc.sql`, `0018_external_identity_linking.sql`) | `tests/google-oidc-transactions.integration.ts` (real Postgres, behavioral) | verified | Single-use via `consumed_at` atomic UPDATE; 900s max TTL enforced. |
| AUTH-OAUTH-002 | `lib/auth/google-oidc-reference.ts:298-412` (`completeGoogleOidcCallback`) | `idoc.google_oauth_transactions` | `tests/google-oidc-transactions.integration.ts` (real Postgres, behavioral: replay/expiry/origin/redirect-uri mismatch); `tests/security-e2e/google-oauth.spec.ts` (real browser replay/tamper) | verified | alg=none is rejected via `algorithms:['RS256']` allowlist in `jose.jwtVerify` plus explicit `protectedHeader.alg !== 'RS256'` re-check, both running before `authenticateGoogleIdentity`/`linkGoogleIdentity` (the atomic completion) — but no test forges a malicious alg=none/alt-alg token, so this specific sub-property is implemented-but-unverified while transaction/replay/state handling is verified. |
| AUTH-IDENTITY-004 | `lib/auth/google-account.ts:31-125` (`authenticateGoogleIdentity`, keyed strictly by `issuer`+`subject`) | `idoc.external_identities` (unique `(issuer, subject)`, unique `(provider, user_id)`) | `tests/google-account-linking.integration.ts` (real Postgres, behavioral) | verified | Email/name/picture never used to identify or authorize; email used only for eligibility check on first-time creation. |
| AUTH-IDENTITY-005 | `lib/auth/google-identity-linking.ts` (`atomicLink`/`atomicUnlink`, advisory-lock collision detection, atomic audit+notification); `lib/auth/google-identity-link-evidence.ts` (5-min fresh-evidence cookie); linking gated by password + `requireFreshStepUp('change-security-settings')` in `app/(dashboard)/dashboard/security/actions.ts` | `idoc.external_identities`, `idoc.audit_log`, `idoc.auth_security_notification_outbox` | `tests/google-oidc-adoption.test.ts` (source-inspection); no dedicated behavioral integration/e2e test drives the full linking mutation through Postgres | implemented-but-unverified | Email-equality-alone auto-link is explicitly rejected (`GoogleAccountLinkRequiredError`) and is verified behaviorally in `google-account-linking.integration.ts`/e2e; the *authenticated linking* atomic-persistence/collision path itself lacks a real-Postgres behavioral test. |
| AUTH-OAUTH-003 | `app/api/auth/google/callback/route.ts:99-104`; `lib/auth/google-account.ts:53-118` (eligibility: verified email, no invite/pre-provisioning gate — public signup is IDOC's policy); `lib/auth/mfa/login.ts:25-49` (`beginPrimaryMfa`, gates `setSession`) | `idoc.users`, `idoc.mfa_enrollment_transactions`/`mfa_challenge_transactions` | `tests/live-privileged-mfa.test.ts` (asserts `authenticateGoogleIdentity` body never calls `setSession`); `tests/security-e2e/google-oauth.spec.ts` (real browser, ordinary-user path only) | verified | Public signup is the documented eligibility policy (docs/05 §5); `setSession` is called only after `beginPrimaryMfa` returns false, so primary auth precedes MFA gating exactly as required. Privileged-account Google login path itself not exercised e2e (only source/unit level). |
| AUTH-INVITE-001/002/003 | NOT FOUND | N/A | N/A | not-applicable | IDOC has no organization/application-scoped invitation concept. The `invitations` table and `'invite-privileged-user'`/`ActivityType.INVITE_TEAM_MEMBER` enum values are genuinely unreferenced dead code (`lib/db/schema.ts`; `grep`-confirmed zero call sites outside schema/type definitions). Privileged accounts are provisioned by trusted Super-Admin action (`grantApplicationRole`), a documented alternative to invitations, not a gap — see AUTH-AUTHZ-005. |
| AUTH-TENANT-001/002 | **Fixed in a later pull request; a Codex review finding on that PR corrected an unverifiable claim about production data.** `getTeamForUser()`'s call site in `app/layout.tsx`'s SWR `fallback` prop was removed, along with the now-fully-dead `getTeamForUser`/`getTeamByStripeCustomerId`/`updateTeamSubscription`/`getUserWithTeam` functions in `lib/db/queries.ts` (each had zero remaining callers once the layout call was removed). | `idoc.teams`, `idoc.team_members` — schema tables left in place, dropping them is a separate decision. **Correction:** an earlier draft of this row and the fix PR's changelog claimed "no production code path ever populated `team_members`." Repository history contradicts that: the original SaaS-starter signup path called `db.insert(teamMembers)` until commit `a3192f1` ("Remove legacy team mutation actions"), and `docs/08`'s own status line records "raw subscription starter deployed" as a past real state. Whether the production database still holds residual rows from that period is not knowable from repository state and is **not** asserted either way here — see the residual-risk note below. | `tests/authorization-boundary-inventory.test.ts` ("the SaaS-starter team-query code path is fully removed, not merely unrouted" — source-inspection proving neither the layout nor `queries.ts` reference these functions/tables any longer); no test/audit exists of the actual production database's current contents | not-applicable | IDOC is single-tenant/single-application by product design, and ordinary per-owner data isolation (member-vs-member, BOLA-style) is correctly handled elsewhere by `requireOwnerOrAdmin`/`authenticatedActor` in `lib/membership/data-access.ts` (in-scope of AUTH-AUTHZ-003, not AUTH-TENANT-*). The **code** path is genuinely gone (verified by source inspection), which is what this canonical requirement is about — this status does not depend on, and should not be read as, a claim about whether residual historical rows exist in the deployed database. That is a separate, unresolved operational question (see the residual-risk note below), not a code-control gap. |
| AUTH-AUTHZ-003 | `lib/membership/data-access.ts:38-68` (`authenticatedActor`/`requireAccountAccess`, fresh DB read of identity/roles/entitlement, `mayAccessAccountFunction` policy predicate); `lib/membership/authorization.ts` (`requireOwnerOrAdmin`, `requireAdministrator`, `requireSuperAdmin`) | `idoc.application_roles`, `idoc.memberships`, `idoc.users.account_state` | `tests/authorization-boundary-inventory.test.ts` (source-inspection: every Server Action/Route Handler inventoried and proven to funnel through `requireAccountAccess`); `tests/membership-foundation.test.ts` (behavioral, real Postgres per docs/21 AUTH-AUTHZ-001/003/004) | verified | Existence of an inaccessible profile is concealed (`getPrivateMember` returns `null` before ownership is even checked when the profile doesn't exist), consistent with AUTH-API-004. |
| AUTH-AUTHZ-004 | `lib/db/schema.ts:177-187` (`applicationRoles`, CHECK `role IN ('member','administrator','super_admin')`); `lib/membership/authorization.ts:14-24` | `idoc.application_roles` (migration `0002_blue_the_anarchist.sql:119`) | `tests/membership-foundation.test.ts` | verified | Matches canonical single-app model exactly: member/admin/super-admin, no tenant-scoped role is grantable (`organization-leader` is dead code, never in `GRANTABLE_ROLES`). |
| AUTH-AUTHZ-005 | `lib/membership/role-grants.ts:28-107` (`grantApplicationRole`/`revokeApplicationRole`: Super-Admin-only actor, `sessionVersion` increment on target, last-Super-Admin protection via `SELECT ... FOR UPDATE` lock, atomic audit+notification); step-up gate in `app/(dashboard)/admin/members/actions.ts:20-28` (`requireFreshStepUp(actor, 'change-privileged-permissions', ...)`) | `idoc.application_roles`, `idoc.audit_log`, `idoc.auth_security_notification_outbox` | No dedicated behavioral test found exercising `grantApplicationRole`/`revokeApplicationRole` against real Postgres (concurrency/last-Super-Admin protection unverified by test) | implemented-but-unverified | "Forbid tenant grants of Super Admin" is not-applicable (no tenants). Self-elevation is structurally impossible (only an existing Super Admin can call this, and it requires fresh step-up), but no test proves the last-Super-Admin concurrency lock actually holds under contention. |
| AUTH-STEPUP-003 | `lib/auth/mfa/step-up.ts:82-125` (`requireFreshStepUp`) and `lib/auth/mfa/decision.ts:24-34` (`sensitiveActionRequiresFreshStepUp`) | `idoc.mfa_challenge_transactions` (purpose=`step-up`), signed `idoc_fresh_step_up`/`idoc_pending_step_up` cookies (not DB-persisted authority) | `tests/live-privileged-mfa.test.ts`; docs/05 §15.5 operator UAT checklist (manual, not automated) | implemented-but-unverified | Bound to exact action/session/user/version/role, single-use (`consumeStepUpAuthority`), remembered devices never satisfy it (`configuredFactor` ignores `rememberTotpDevice`). No automated integration/e2e test found driving `requireFreshStepUp` end-to-end against real Postgres. |
| AUTH-SESSION-009 | `lib/auth/session.ts:40-93` (`registeredSessionIsValid`, `setSession`); `lib/auth/session-registry.ts`; `sessionVersion` incremented on role grant/revoke (`role-grants.ts:45-48,91-94`) | `idoc.auth_sessions`, `idoc.users.session_version` | `tests/persisted-session-registry.test.ts`, `tests/canonical-session-lifecycle.test.ts`, `tests/session-token-boundaries.test.ts` (behavioral); `tests/security-e2e/session-replay.spec.ts` (real browser/HTTP) — per docs/21 AUTH-SESSION-009 | verified | Legacy pre-registry cookie fallback was found and removed in a prior hardening pass (docs/21 §8i); current code path requires both a valid signature and a matching registry row + version. |
| AUTH-REDIRECT-003 | `lib/auth/google-oidc-reference.ts:135-147` (`validRedirectUri`, server-config-only, HTTPS-or-loopback) and `:214-227` (`normalizeReturnTo`, same-origin-only internal path); callback route is the fixed `/api/auth/google/callback` path, never client-supplied | N/A | `tests/google-oidc-transactions.integration.ts` (behavioral: mismatched `redirect_uri`/origin rejected) | verified | Callback destination is entirely server-owned config (`GOOGLE_OAUTH_REDIRECT_URI`); no provider-redirect-hint or client-selected callback path exists anywhere in the OAuth code. |
| AUTH-OPERATIONS-005 | `lib/auth/google-oidc-reference.ts:160-168` (`resolveGoogleJwks`, `jose.createRemoteJWKSet` — bounded built-in key caching/refresh); any JWKS/verification failure throws inside the `try` at `:379-411`, converted to `GoogleOidcError('invalid_id_token')`, never exposing raw provider text (`google_oauth_callback_failed` logs only a category, per `google-oauth-failure-alerting.test.ts`) | N/A | `tests/google-oauth-failure-alerting.test.ts` (source-inspection: no raw error interpolated into alert/log); no test forces an actual JWKS-unreachable or malformed-key scenario | implemented-but-unverified | Fails closed by construction (any thrown error → `GoogleOidcError`), but no behavioral test simulates a JWKS outage/unknown-key-refresh scenario to prove the failure path actually rejects rather than silently succeeding. |
| AUTH-SECRET-001 | `lib/auth/google-oidc-reference.ts:81-85,149-155` (`GOOGLE_OAUTH_ENV.clientSecret` read server-only via `required()`); never referenced in any client component/`NEXT_PUBLIC_*` var (grep-verified across repo) | N/A (Vercel env var, not DB) | `tests/google-oidc-adoption.test.ts` (source-inspection: `.env.example` documents the variable name only, no value) | implemented-but-unverified | Docs/07 §15.1 documents rotation procedure (client/secret pair via Google console overlap). No automated test scans build output for the secret value (docs/05 "Production build and runtime boundary" describes a scan but it wasn't located/confirmed as an executable test in this pass). |
| AUTH-API-004 | `app/api/user/route.ts` (denial → generic `null`, 200, not error-shaped); `lib/membership/authorization.ts` (`AuthorizationError`, generic message); `lib/membership/data-access.ts:70-74` (`getPrivateMember` returns `null` for both "doesn't exist" and would-be-denied cases before throwing) | N/A | `tests/authorization-boundary-inventory.test.ts` (source-inspection only) | implemented-but-unverified | Pattern is consistent everywhere reviewed (generic `AuthorizationError` message, no resource-existence leakage), but no behavioral test asserts response bodies/status codes for a real cross-account request at the HTTP layer. |


## Stage 6 — Logging, Audit, Operations, Crypto-Lifecycle, Privacy, Dependency-Failure

Methodology note: docs/21 uses its own AUTH-* numbering scheme that does **not** correspond 1:1 to the canonical IDs below — I mapped by reading the actual content, not by ID match, and opened every file cited.

| ID | Implementation location | Persistence location | Test evidence | Operational evidence | Assessment | Notes |
|---|---|---|---|---|---|---|
| AUTH-LOG-001 | `lib/observability/logger.ts:11-17` (categorical event+meta), `lib/observability/request-id.ts` (server-generated correlation ID, `middleware.ts` assigns `crypto.randomUUID()`, never from client header) | `idoc.audit_log` (distinct from console logs) | `tests/security-e2e/cookies-and-headers.spec.ts` (proves `x-request-id` server-generated, not client-reflected) | None found beyond code comments | `partial` | Correlation/attribution exists; no formal "stable taxonomy" doc of security-event names, tenant field N/A (single-tenant), minimized metadata enforced by convention not a schema. |
| AUTH-LOG-002 | Convention enforced via `lib/observability/logger.ts` + manual review; no schema-level redaction filter | `idoc.audit_log`, `idoc.auth_security_notification_outbox` | `tests/security-completeness-item-8.test.ts`, `tests/evidence-safety-scan.integration.ts` (full-content scan of every evidence table for password/otp/secret/digest/token leakage) | None | `verified` | Real behavioral test scans persisted evidence tables end-to-end for secret material; strong evidence. |
| AUTH-LOG-003 | `lib/observability/logger.ts`; `lib/security/password-breach-check.ts:40,50` (documented plain-`console.warn` exception, no request/response body dumps anywhere) | N/A (logs are ephemeral, not a DB table) | `tests/security-completeness-item-8.test.ts` (source-inspection for categorical-only call sites) | None (no log-retention/SIEM policy documented) | `partial` | Categorical/redacted logging confirmed; "retention-classed" and "tenant-isolated" have no explicit implementation since there's a single tenant and no log-retention policy document. |
| AUTH-AUDIT-001 | `lib/membership/status-actions.ts` (suspend/reinstate/correct), `lib/membership/data-access.ts:202-211` (deletion), `lib/membership/role-grants.ts`, `lib/auth/mfa/*` — every sensitive mutation inserts `auditLog` in the same DB transaction, actor-attributed via `actorId` | `idoc.audit_log` (append-only, see AUTH-AUDIT-002) | `tests/audit-evidence.integration.ts`, `tests/evidence-safety-scan.integration.ts` | None additional | `verified` | Actor-attributed, transactional, privacy-minimized (evidence-safety-scan proves no secrets), distinct from console logs. |
| AUTH-AUDIT-002 | `lib/db/migrations/0002_blue_the_anarchist.sql:127-135` — `idoc.reject_immutable_history_change()` trigger, `BEFORE UPDATE OR DELETE ON idoc.audit_log` / `profile_change_history`, raises exception for **any** role, not application-convention only | Migration `0002_blue_the_anarchist.sql` | Covered indirectly by every integration test writing audit rows; no test that specifically attempts an `UPDATE`/`DELETE` against `audit_log` and asserts the trigger fires | docs/05 §3: "Database triggers reject updates and deletes to audit and profile-change history." | `verified` | DB-level, not app-level — a privileged actor (even a superuser DB role without `BYPASS RLS`/trigger-disable) cannot silently rewrite. Genuine strength. One gap: no direct behavioral test proves the trigger itself fires (only proven by code inspection). |
| AUTH-AUDIT-003 | `lib/membership/data-access.ts:214-219` (`listAuditHistory` requires `requireAdministrator`); all audit writes occur inside the same DB transaction as the mutation (e.g. `status-actions.ts:62-74`) — so if the audit insert fails, the whole transaction (including the mutation) rolls back, i.e. fail-closed by construction, not a "bounded durable fallback" | `idoc.audit_log` | `tests/audit-evidence.integration.ts` | None | `verified` | Audit read is authorized (`requireAdministrator`); audit write failure fails the mutation closed via the same transaction — meets "fail closed" without needing a separate fallback queue. |
| AUTH-OPERATIONS-006 | `lib/notifications/breached-password-alert.ts`, `lib/notifications/google-oauth-failure-alert.ts`, `lib/notifications/profile-change-delivery.ts` — each sends a flat email to `IDOC_ADMIN_NOTIFICATION_EMAIL` for its own specific event. `lib/notifications/alert-severity.ts` (added by the alert-severity-taxonomy pull request) now assigns each a fixed informational/warning/high/critical severity, and every alert's subject line is tagged with it (e.g. `[HIGH] IDOC: breached password rejected`) | N/A | `tests/alert-severity.test.ts` (proves the fixed severity mapping and that every alert call site actually tags its subject, not just that the helper function works in isolation) | docs/07 §12 "Routine operational checks" table (daily/weekly/monthly/quarterly manual review cadence) | `partial` | The severity-taxonomy half of this requirement is now real and tested. **No correlation/anomaly-detection engine exists**, and this pull request does not attempt one — that remains a genuinely larger, separate capability (pattern/frequency detection across events), not something to claim by proxy. Flagging this half as still missing rather than softening it. |
| AUTH-OPERATIONS-007 | Session/device/factor revocation exists (`revokeAllUserSessions`, `forgetAllLoginDevices` in `app/(login)/actions.ts:239-240`) for **self-service** account compromise (password reset, recovery). No code path for **operator-initiated** infrastructure/account compromise response (e.g. an admin-triggered "revoke this user's everything" tool) | `idoc.auth_sessions`, `idoc.login_trusted_devices`, `idoc.mfa_factors` | `tests/account-security-management.test.ts:58-61` (self-service deletion path only) | docs/07 §11 "Security incident escalation": "Suspected unauthorized administrator access: revoke affected sessions/credentials and escalate immediately." / "Suspected secret leakage: rotate the affected Vercel, Render PostgreSQL, application-authentication, or Stripe secret, then investigate logs and the exposure window." | `partial` | Genuine incident-response runbook section exists (§11) with named actions per scenario, but it is a manual instruction list ("revoke... and escalate"), not a tool/script — an administrator has no dedicated "force-revoke all authority for user X" admin action in code; they'd have to improvise via existing per-user session-revocation primitives. Evidence preservation and incident-correlated audit events are not explicitly wired for this scenario. |
| AUTH-LIFECYCLE-005 | `lib/db/schema.ts:25,34` — `users.accountState` CHECK `('unverified','onboarding','active','suspended','migrated_pending','deleted')`, server-owned (only set via `data-access.ts`/`status-actions.ts` transactions, never client-writable); `memberships.status` is a separate CHECK enum distinguishing club-membership state from account authentication state | `idoc.users.account_state`, `idoc.memberships.status` | `tests/audit-evidence.integration.ts:130-139` | None additional | `verified` | Clean separation: account authentication state (`users.account_state`) vs. club-membership entitlement state (`memberships.status`) are two independent, server-owned enums. No "org membership" concept exists (single-tenant). |
| AUTH-LIFECYCLE-006 | **Fixed in a later pull request; hardened per two Codex review findings on that PR.** `app/(login)/actions.ts:231-243` — deletion sequence unchanged (`deleteOwnAccount()` → `revokeAllUserSessions` → `forgetAllLoginDevices` → `clearSession`). Admin-initiated disablement now exists separately: `lib/membership/account-suspension.ts` `suspendUserAccount`/`reinstateUserAccount` (`requireAccountAccess('administration')` + `requireAdministrator`), sets/clears `users.account_state='suspended'`, bumps `session_version`, revokes all sessions and login-trusted devices, writes audit + `account_suspended`/`account_reinstated` notifications. Rejects suspending a user who currently holds an `administrator`/`super_admin` grant. **P2 fix 1:** `grantApplicationRole` (`lib/membership/role-grants.ts`) now takes the same `users` row lock before touching `application_roles` at all, closing a real race where a concurrent grant's `application_roles` INSERT (a different table, not blocked by suspension's read of it) could commit after suspension's read but before suspension's own `users` update released the lock, leaving a suspended account with an active grant. **P2 fix 2:** both functions are now retry-idempotent — a retry after a partial failure (the transactional state/audit write commits, but the session/device revocation or notification-enqueue step afterward throws) completes the still-missing side effects instead of being rejected outright as "already suspended"/"not currently suspended," using the DB-committed transition timestamp (not call-time `Date.now()`) as the notification dedupe key so a retry converges rather than duplicating. | `idoc.users`, `idoc.auth_sessions`, `idoc.login_trusted_devices`, `idoc.audit_log`, `idoc.auth_security_notification_outbox`, `idoc.application_roles` | `tests/account-suspension.integration.ts` (real Postgres, 10 tests, including idempotent-retry coverage for both directions with duplicate-audit/duplicate-notification assertions); `tests/role-grants.integration.ts` (2 new tests: granting a role to a suspended/deleted account rejected; a concurrent grant-vs-suspend race, run for real via `concurrently()`, always leaves the DB in a consistent state — never a suspended account with an active grant) | None | `verified` | Distinct from `memberships.status='suspended'` (club-entitlement, unaffected) — the admin UI (`app/(dashboard)/admin/members/page.tsx`) labels the two separately to avoid operator confusion. |
| AUTH-LIFECYCLE-007 | N/A | N/A | N/A | N/A | `not-applicable` | IDOC is a genuinely single-tenant platform: no organization/multi-tenant membership concept exists anywhere in schema or code (`memberships` = club entitlement, not org membership; single `applicationId='idoc-web'` throughout MFA/session code). Confirmed independently — docs/21 contains zero mentions of "tenant"/"organization". |
| AUTH-STORAGE-007 | Logical invalidity is enforced on every read: `session-registry.ts:38-56` (`revoked_at is null and absolute_expires_at > now()`), `mfa_enrollment_transactions`/`mfa_challenge_transactions` expiry checks in `totp.ts:211-217`. `lib/security/data-retention-purge.ts` (added by the data-retention-purge-job pull request) now physically deletes rows from all six named tables once each row's own `expires_at` is more than 30 days past, run daily via `app/api/cron/data-retention-purge/route.ts` (registered in `vercel.json`, gated by the same shared-secret bearer-token pattern as every other cron route) | `idoc.email_otp_codes`, `idoc.mfa_challenge_transactions`, `idoc.mfa_enrollment_transactions`, `idoc.account_tokens`, `idoc.auth_sessions`, `idoc.login_trusted_devices` | `tests/data-retention-purge.integration.ts` (real-Postgres: proves rows past the 30-day grace period are deleted from all six tables, rows within it or not yet expired are not, and an empty-database run is a safe no-op) | `verified` | Both halves of the requirement now hold: logical invalidity is solidly enforced (unchanged from before), and physical cleanup now exists, is scheduled, and is behaviorally proven. The 30-day grace period is a conservative engineering default for incident-investigation headroom, not a compliance-mandated value — a revoked-but-not-yet-expired row (e.g. an admin-revoked session) is purged on the same schedule as a naturally-expired one, once its own expiry passes the same window. |
| AUTH-SECRET-002 | `lib/runtime/configuration.ts` (every secret is `server-only`, never `NEXT_PUBLIC_*`, fails closed if malformed/absent) | N/A | `tests/runtime-configuration.test.ts` | docs/07 §15.1 "Authoritative inventory" — a full table of every secret, its consumer, format, and rotation rule; §12.1 "never paste them in tickets, PRs, logs, screenshots or chat" | `verified` | This is a genuinely strong, explicit secret inventory with per-secret ownership/rotation rules — one of the best-covered items in this stage. |
| AUTH-SECRET-003 | `lib/runtime/configuration.ts:82-114` (`mfaConfiguration` — active key ID must exist in the ring; old IDs remain valid for decrypt but only active ID is used for new encryption, per `encryptTotpSecret`/`totp.ts:116-123` always using the caller-supplied active key) | `idoc.mfa_factors.encryption_key_id` | `tests/runtime-configuration.test.ts:115-124` (invalid/absent active key fails closed) | docs/07 §15.2 "Safe TOTP encryption rotation is strictly additive" (5-step procedure); §11 "Suspected secret leakage: rotate... then investigate" | `partial` | New-operations-use-current-material and bounded prior-generation support are implemented and documented. "Compromise stops acceptance and triggers audited response" is **not** implemented: there's no mechanism to mark a specific key ID compromised and block it from decrypting existing factors while blocking only new encryption under it — the only lever is full removal (which the runbook says "locks out that factor" for everyone, an all-or-nothing operation), and no audit event is generated for a compromise-driven key removal. |
| AUTH-CRYPTO-004 | `lib/db/schema.ts:453` (`mfa_factors.encryption_key_id`), `lib/runtime/configuration.ts:92-107` (key ring as a Map, active-vs-known-but-inactive keys) | `idoc.mfa_factors.encryption_key_id` | `tests/runtime-configuration.test.ts` | docs/07 §15.1/§15.2 | `partial` | Non-secret key **version** identification exists (`encryption_key_id` per factor, active-key-ID config). There is no explicit "pending/active/retiring/retired/compromised" lifecycle state machine anywhere — key state is implicit (present-in-ring = usable, absent = fails closed for that key ID), with no distinct "retiring" vs "retired" vs "compromised" semantics and no code to reject a specific key ID as compromised while accepting others. |
| AUTH-CRYPTO-005 | `lib/auth/mfa/totp.ts:116-138` (`encryptTotpSecret`/`decryptTotpSecret`, keyId-prefixed serialization, AES-256-GCM, `resolveKey(keyId)` callback pattern enabling multi-key support); session keys: `AUTH_SECRET` is a **single** key with no ring (`lib/runtime/configuration.ts:47`) | `idoc.mfa_factors.encryption_key_id` | `tests/canonical-mfa-runtime.test.ts:163-171` (encrypt/decrypt roundtrip with keyId plumbing verified), `tests/live-privileged-mfa.test.ts:82-84` (fail-closed on malformed ring) | docs/07 §15.2 5-step additive-rotation procedure; §15.5 UAT checklist item "Add/switch a second TOTP key: old factor works and newly replaced factor records the new ID" | `partial` | TOTP encryption genuinely supports multi-key rotation (not just single-key) — this is real and verified at the unit level. However: (1) no integration test actually builds a two-entry key ring via `mfaConfiguration()` and proves an old-key-encrypted factor still decrypts while a *different* key is active — only unit-level `resolveKey` plumbing is tested; (2) **session keys (`AUTH_SECRET`) have zero rotation support** — docs/07 §15.1 states plainly "No old-key ring exists, so coordinate a forced sign-in," meaning `AUTH_SECRET` rotation is a hard cutover, not "bounded verification policy" rotation as the canonical requirement implies; (3) migrate-fail-closed and no-plaintext-telemetry are both true (`decryptTotpSecret` throws on bad key, no plaintext ever logged — confirmed no TOTP secret appears in any log call site). |
| AUTH-SECRET-004 | Pepper: **not implemented** — `lib/auth/password-hash.ts` has no pepper concept at all (Argon2id with per-password salt only). OAuth client secret: `lib/auth/google-oidc-reference.ts:83` — single `GOOGLE_OAUTH_CLIENT_SECRET` env var, no application-level dual-secret/overlap support | N/A | NONE | docs/07 §15.1 Google secret row: "Rotate the secret through an overlap/cutover supported by Google; existing IDOC sessions are unaffected, in-flight OAuth may fail." | `partial` | Pepper is explicitly optional in the canonical requirement and genuinely absent here — reasonable to treat as **not-applicable** for the pepper half (never claimed, never implemented, no false "implemented" claim). OAuth client secret rotation relies entirely on Google's provider-side overlap window plus manual operator coordination — no application code tracks a rollback/retirement audit trail for the secret itself, though it is confirmed never exposed to the client. |
| AUTH-EMAIL-005 | `lib/notifications/account-delivery.ts:105` and `lib/notifications/profile-change-delivery.ts:27` both construct links via `new URL(path, baseUrlForServer())` — `baseUrlForServer()` reads only `BASE_URL` server config (`lib/runtime/configuration.ts:48-55`), never `request.headers.get('host')` or any client-controlled value; Google callback binding in `lib/auth/google-oidc-reference.ts`/`google-oauth-browser-binding.ts` similarly binds to server-configured redirect URI | N/A | Covered indirectly via `tests/runtime-configuration.test.ts` (BASE_URL validation) | docs/07 §15.1 `BASE_URL` row: "Not secret. Must match across instances and the deployed origin." | `verified` | Grep confirms zero usage of request Host/Origin headers for constructing security email links anywhere in the codebase. |
| AUTH-EMAIL-006 | `lib/notifications/account-delivery.ts` / `account-delivery-worker-core.ts:29-41` — provider send failure only marks the outbox row retryable/dead-lettered; it never marks the underlying token/OTP as consumed or completes any auth mutation (mutation and audit commit in the DB transaction *before* the outbox row is even queued, per e.g. `data-access.ts`/`account-recovery.ts` patterns); enumeration-neutral failure via `equalizeAnonymousResponse` (`lib/security/response-timing.ts`) | `idoc.notification_outbox`, `idoc.account_delivery_outbox` | `tests/response-timing.test.ts`, `tests/evidence-safety-scan.integration.ts` (provider-failure branch: `PROVIDER_FAILURE_MARKER` never persisted) | docs/07 §12.1 "Retry delay is `min(3,600, 30 × 2^(attempt − 1))`... attempt six is retained as dead-lettered and is not claimable again" | `verified` | Delivery/authority separation, bounded exponential retry (6 attempts), idempotent lease-based claiming, and enumeration-neutral failure are all implemented and behaviorally tested. |
| AUTH-EMAIL-007 | `app/api/mailchimp/webhook/route.ts` (added by the bounce-complaint-webhook pull request): verifies Mandrill's HMAC-SHA1 webhook signature (`lib/notifications/mailchimp-transactional-webhook.ts`) before parsing or acting on any event; a hard bounce or spam complaint alerts `IDOC_ADMIN_NOTIFICATION_EMAIL` via the existing severity taxonomy (`lib/notifications/bounce-complaint-alert.ts`, warning/critical respectively); a soft bounce is logged only, never alerted, since it's routine/transient by definition. Deliberately does **not** automatically suppress future sends to a bounced/complaining address — that is a policy decision left to the human reviewing the alert, avoiding the "single-bounce disablement" anti-pattern this row previously flagged as a risk to design around, not a capability the row itself demands | N/A | `tests/mailchimp-webhook-signature.test.ts` (signature verification and event parsing, unit); `tests/mailchimp-webhook-route.integration.ts` (behavioral: unconfigured-key 503, bad-signature 400, hard-bounce/spam alert with correct severity tag and recipient, soft-bounce and unrelated-event no-alert, multi-event batch, malformed-payload-but-valid-signature accepted-but-ignored) | `verified` | Ingestion, signature verification, and categorized alerting are real, tested, and correctly avoid the anti-pattern this row warned against. Signature-algorithm correctness is implemented against Mandrill's documented specification but has not been validated against a live Mandrill webhook delivery — an operator should use Mandrill's own "send test webhook" feature after configuring `MAILCHIMP_TRANSACTIONAL_WEBHOOK_KEY` in the dashboard, before relying on this in production. |
| AUTH-PRIVACY-001 | `lib/membership/data-access.ts` (owner-or-admin checks via `requireAccountAccess`/`requireAdministrator` on every read), `lib/admin/csv.ts` + `app/api/admin/export/*` (admin-only exports) | `idoc.users`, `idoc.profiles`, etc. — single-tenant, so "tenant-isolated" is trivially true | `tests/authorization-boundary-inventory.test.ts` (structural: every Server Action/Route Handler funnels through `requireAccountAccess`) | docs/07 §13 "Data export and reporting" — "Export only the fields necessary for the stated business purpose and avoid distributing raw migration exports or unnecessary billing identifiers." | `partial` | Access control and purpose-limited export guidance exist and are enforced/tested. "Bounded retention-export" and "excluded from unrestricted analytics" have no concrete implementation found — no retention-expiry job (see AUTH-STORAGE-007) and no analytics-exclusion mechanism was located (no analytics integration was found at all, which arguably satisfies "excluded from unrestricted analytics" by absence, but that's not a designed control). |
| AUTH-PRIVACY-002 | **Fixed in a later pull request; a Codex review finding on that PR corrected an unverifiable claim about production data.** `lib/security/rate-limit.ts:15,40-48` (IP is SHA-256/HMAC-digested via `RATE_LIMIT_HASH_KEY`, never stored raw, 15-minute rolling window) for the authentication rate-limit path; `lib/auth/login-device-trust.ts` (device trust is a cryptographic token, not a device fingerprint). The `idoc.activity_logs.ip_address` read/render path (`getActivityLogs()` in `lib/db/queries.ts`, rendered on `/dashboard/activity`) has been removed and is no longer selected or displayed by any application code. | `idoc.account_request_limits` (hash-only, rate-limit path); `idoc.activity_logs.ip_address` (column still exists, no longer read by any current code path). **Correction:** an earlier draft claimed nothing had ever written to this column. Repository history contradicts that: the original SaaS-starter's `logActivity()` function (removed in the same `a3192f1` commit that removed the team-mutation code) populated `ip_address` on every logged activity. Whether the production database holds residual rows with real IP addresses from that period is not knowable from repository state and is **not** asserted either way — see the residual-risk note below. | `tests/authorization-boundary-inventory.test.ts` ("activity_logs.ip_address is no longer read or rendered" — source-inspection proving neither `queries.ts` nor the activity page reference it any longer); no audit exists of the actual production database's current contents | docs/07 "Account-security management operations": "the session registry currently stores only authentication, activity, and absolute-expiry timestamps" | verified | The genuinely-hashed rate-limit path and the removal of the read/render **code** path are both clean and verified. This status is about the code control, not a claim that the column is empty in production — that is a separate, unresolved operational question (see the residual-risk note below). Dropping the unused `ip_address` column itself (a schema/migration change) is left as a separate follow-up decision, not bundled into this fix. |
| AUTH-DEPENDENCY-001 | Postgres: no explicit handling — connection failure throws and requests fail closed (`lib/db/drizzle.ts`, no try/catch/fallback). Email (Mailchimp): `lib/notifications/mailchimp-transactional.ts:23` throws on non-OK, caught by the outbox retry/dead-letter worker. Turnstile: `lib/auth/turnstile.ts:16-50` explicit fail-closed (missing config, provider error, hostname/action mismatch all return `false`). HaveIBeenPwned: `lib/security/password-breach-check.ts:24-55` explicit fail-**open**, with an inline code comment justifying it as defense-in-depth, not primary control. Google OAuth: no explicit fail-open/closed comment found, but token/JWKS validation failures naturally fail the login attempt closed. | N/A | `tests/turnstile-contract.test.ts`, `tests/password-breach-check.test.ts` | docs/07 §12 "Failed Stripe webhooks, renewal failures... application errors" (daily check) | `partial` | Each dependency's failure posture is deliberate and mostly documented in code comments (a real strength), but there's no single unified "dependency risk register" doc, and Postgres itself has no explicit degrade/queue strategy beyond "the whole request fails" (arguably correct fail-closed behavior for an authoritative store, but undocumented as an intentional choice). |
| AUTH-DEPENDENCY-002 | `lib/auth/session.ts` + `lib/auth/session-registry.ts` — every protected data path re-verifies against `idoc.auth_sessions`/`sessionVersion` in Postgres; docs/21 §"session cookie's authentication acceptance path" records that a **legacy cookie-only fallback was deliberately removed** in a later PR specifically to close this exact canonical gap | `idoc.auth_sessions` | `tests/security-e2e/session-replay.spec.ts` (per docs/21 AUTH-SESSION-010 row) | None additional | `verified` | Direct evidence this exact canonical requirement drove a real code change (removal of a JWT-only fallback) — strong, specific evidence rather than an assumption. |
| AUTH-OPERATIONS-008 | `idoc.auth_sessions` (single shared Postgres instance — no per-instance state), `idoc.account_request_limits` (shared rate-limit buckets), role checks always re-read from `idoc.application_roles` per request (`lib/membership/data-access.ts`) — cross-instance consistency follows from a single authoritative database with no local caching layer found | `idoc.auth_sessions`, `idoc.application_roles`, `idoc.account_request_limits` | Not specifically tested for multi-instance race conditions beyond the general test suite | None found for "monitored trusted UTC time with bounded skew" specifically — `now()` is always the Postgres server clock, not app-server clocks, which is one implicit mitigation | `partial` | Cross-instance consistency is achieved architecturally (single Postgres source of truth, no local/in-memory caches for revocation/roles/rate-limits found), but there's no explicit clock-skew monitoring or alerting — reliance is entirely implicit on Postgres's own clock via `now()`. |
| AUTH-STORAGE-008 | **NOT FOUND** — no backup/restore code, script, or migration of any kind in this repository | NOT FOUND | NONE | docs/05 §2 "Backups: Enable and verify recoverable database backups appropriate to the production tier." (requirement statement only); docs/07 §12 quarterly check: "Render PostgreSQL backup/recovery posture" (a checklist line item, not a procedure); docs/07 §14 references an "archival export/backup" for WordPress decommissioning only, unrelated to ongoing production DB backup | `missing` | This is correctly flagged as missing, not softened. The only operational evidence is a one-line quarterly "check the posture" reminder and a generic requirement statement — neither is an actual documented backup/restore/reconciliation procedure, and there is zero code/schema/script for backup, encryption-at-rest verification, retention bounds, or restore-time reconciliation logic (nothing prevents a restore from resurrecting a revoked session/deleted account/compromised key, because no restore procedure exists to review). |
| AUTH-OPERATIONS-009 | `lib/runtime/configuration.ts` — every required(), secret(), url(), base64Key() helper throws `Invalid production configuration: <NAME>` (generic, non-revealing message — never echoes the actual value or which check subtype failed) on missing/malformed/placeholder values; `mfaConfiguration()`/`accountDeliveryConfiguration()`/`privilegedProductionConfiguration()` validate the full secret/key/origin/schema surface | N/A | `tests/runtime-configuration.test.ts`, `tests/live-privileged-mfa.test.ts`, `tests/build-runtime-boundary.build.ts` (proves build succeeds without secrets while runtime fails closed) | docs/07 §"Production runtime configuration boundary": "A deployment build intentionally succeeds without these values, while each privileged runtime boundary fails closed until its real configuration exists." | `verified` | This is a genuinely strong, well-tested fail-closed startup/readiness gate — one of the strongest items in this stage. Error messages are generic (`Invalid production configuration: AUTH_SECRET.`) and never leak the actual secret value or deployment topology. |
| AUTH-OPERATIONS-010 | `.github/workflows/auth-security-verification.yml` (install → `pnpm audit` → `pnpm typecheck` → `pnpm test:ci` → Playwright security e2e), `.github/workflows/release-1-verification.yml` (install → `pnpm check:release1` = typecheck+test+build) | N/A | The workflows themselves are the test evidence | None | `partial` | Dependency install, typecheck, unit/integration tests, and build are all real, enforced CI gates on every PR. **No whitespace/lint check and no "contract documentation change" validation step were found** — no ESLint/Prettier config referenced in CI, no doc-drift check verifying docs/21 or docs/05 stay in sync with code changes. This is a genuine partial gap, not "verified." |
| AUTH-OPERATIONS-011 | **NOT FOUND** as a distinct machine-readable checklist — the closest artifacts are docs/07 §15.6 "Release signoff (leave unchecked until manually proved)" (a markdown checkbox list) and docs/11 (release-1-verification-matrix.md, not opened in this pass) | N/A | The `check:release1`/`test:security` CI gates are automated *tests*, not a readiness *checklist* | docs/07 §15.5/§15.6 — an explicit, manually-checked markdown checklist covering security config, MFA, sessions, email delivery, etc., **correctly framed as "leave unchecked until manually proved"** rather than an automatic certification | `partial` | The checklist exists and is explicitly non-self-certifying (a real strength — it doesn't silently claim security), but it's a markdown document requiring a human to tick boxes, not a machine-readable evidence artifact (e.g. no JSON/YAML manifest, no CI step that fails a PR if the checklist itself is stale). |
| AUTH-SESSION-010 | `lib/auth/session-registry.ts:38-56,116-134` — `readActiveSession`/`listActiveSessions` require `revoked_at IS NULL AND absolute_expires_at > now() AND session_version = <current>` on every read; `middleware.ts` is explicitly documented (docs/21 AUTH-SESSION-010 row) as a non-authoritative UX layer only | `idoc.auth_sessions`, `idoc.users.session_version` | `tests/canonical-session-lifecycle.test.ts`, `tests/security-e2e/session-replay.spec.ts` | None additional | `verified` | Directly corroborated by docs/21's own row for this exact ID, which I independently confirmed by reading `session-registry.ts` and the middleware boundary. |
| AUTH-API-005 | `lib/notifications/account-delivery-worker-core.ts:29-41` — cron routes return only aggregate counts (`{delivered, retryable, deadLettered, ineligible, leaseLost}` shape) or `{error: 'Unauthorized'}`/`{error: 'Worker failed'}`, never topology/secret detail; constant-time bearer-token auth via `timingSafeEqual`. `app/api/health/route.ts` (added by the health-readiness-endpoint pull request) is a dedicated, unauthenticated liveness/readiness route: it confirms the database is reachable (`select 1`) and returns only `{status: 'ok'}`/`{status: 'error'}`, with `Cache-Control: no-store` | N/A | `tests/health-endpoint.integration.ts` proves the 200/no-store/minimal-body shape against a real database; cron response minimization remains verified only by source inspection | docs/07 §12.1: "Cron responses expose only aggregate delivered, retryable, dead-lettered, ineligible, and lease-lost counts." | `verified` | Cron/delivery endpoints are well-minimized and documented, and the previously-absent health/readiness route now exists, is minimal, and is tested. |

### Stage 6 summary of the most consequential gaps (do not soften)

1. **AUTH-STORAGE-008 (backups)** — genuinely `missing`. No backup/restore code, script, or documented step-by-step procedure exists; only a one-line quarterly "check the posture" reminder.
2. **AUTH-EMAIL-007 (bounce/complaint handling)** — genuinely `missing`. Zero code or docs on the topic.
3. **AUTH-OPERATIONS-006 (alert severity taxonomy)** — `missing`. Only three ad hoc single-event admin emails exist; no correlation/severity system.
4. **AUTH-STORAGE-007 (physical cleanup)** — `partial`. Logical invalidation is solid and verified, but no purge job exists for any expired-record table, so several tables grow unbounded.
5. **AUTH-LIFECYCLE-006 — fixed in a later pull request.** At the time of this stage audit, `users.account_state = 'suspended'` was defined in the schema's CHECK constraint but no code path set it. `lib/membership/account-suspension.ts` now provides admin-initiated suspend/reinstate actions; see the AUTH-LIFECYCLE-006 row above, now `verified`. Kept here as a record of what was true at the time of this stage's original audit, not a currently-open gap.
6. **AUTH-CRYPTO-005 / AUTH-SECRET-003** — TOTP key rotation mechanism is real and better than a naive single-key design, but session-key (`AUTH_SECRET`) rotation has no ring/overlap at all (documented hard cutover), and there is no "compromised key" state distinct from "removed key."
7. **AUTH-OPERATIONS-009** and **AUTH-AUDIT-002** are the strongest items in this stage — both are genuinely `verified` with real fail-closed code and a DB-level immutability trigger, respectively.
